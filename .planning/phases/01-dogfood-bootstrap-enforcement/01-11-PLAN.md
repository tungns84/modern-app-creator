---
phase: 01-dogfood-bootstrap-enforcement
plan: 11
type: execute
wave: 3
depends_on: ["01-04", "01-05"]
files_modified:
  - .claude/skills/plan/SKILL.md
  - .claude/skills/verify/SKILL.md
  - .claude/skills/new-module/SKILL.md
  - .claude/skills/new-feature/SKILL.md
  - .claude/skills/design-implement/SKILL.md
  - scripts/checks/settings-lint.mjs
  - scripts/checks/skills-lint.mjs
  - scripts/checks/meta-link-lint.mjs
  - scripts/checks/tests/lints.test.mjs
  - Taskfile.yml
autonomous: true
requirements: [AGENT-02, AGENT-04, AGENT-09]
must_haves:
  truths:
    - "Five skills exist under .claude/skills/ with valid frontmatter; `plan` and `verify` are fully implemented, the other three are skeleton contracts (D-21)"
    - "The `plan` skill emits specs/NNN-slug/plan.md in AI-COWORK §3.2 format with tier declaration and computes the next NNN from existing specs/ dirs"
    - "The `verify` skill is a thin wrapper over `task verify` and summarizes failures by violated rule"
    - "`task verify` runs every gate that exists in Phase 1 and prints per-gate timing lines per the Q-004 contract; `task verify:fast` is the static-only subset (D-19)"
    - "settings-lint proves the T1 allowlist exists and permissions.deny stays in lockstep with tiers.json t4 patterns; meta-link-lint fails on any .planning/ reference inside shipped dirs (D-06)"
  artifacts:
    - path: ".claude/skills/plan/SKILL.md"
      provides: "Full plan skill (H2 input producer)"
      contains: "Approved-by"
    - path: ".claude/skills/verify/SKILL.md"
      provides: "Full verify skill (wrapper over task verify)"
      contains: "task verify"
    - path: ".claude/skills/new-module/SKILL.md"
      provides: "Skeleton contract — T3, pre-filled plan.md template, implemented Phase 2"
      contains: "tier: T3"
    - path: "scripts/checks/settings-lint.mjs"
      provides: "Allowlist presence + deny↔tiers lockstep check (AGENT-04)"
    - path: "scripts/checks/meta-link-lint.mjs"
      provides: "D-06 separation enforcement (Pitfall 10)"
    - path: "Taskfile.yml"
      provides: "verify + verify:fast aggregation with per-gate timing (Q-004 contract)"
      contains: "verify:fast"
  key_links:
    - from: "Taskfile.yml verify task"
      to: "scripts/checks/*.mjs and node --test suites"
      via: "single verify path shared by CI, the verify skill, and devs"
      pattern: "node --test"
    - from: ".claude/skills/verify/SKILL.md"
      to: "Taskfile.yml"
      via: "thin wrapper rule (Pattern 7)"
      pattern: "task verify"
    - from: "scripts/checks/settings-lint.mjs"
      to: ".claude/settings.json and .cowork/tiers.json"
      via: "lockstep check between generated deny rules and the tier source (D-22)"
      pattern: "t4"
---

<objective>
Complete the Claude Code pack and the verify spine: the five skills (D-21 depth split), the three remaining lint checks (settings, skills, meta-link), and the `task verify` / `task verify:fast` aggregation implementing the Q-004 timing contract. Spec units: specs/005-claude-pack and specs/001-hooks-enforcement (approved in 01-01).

Purpose: The dogfood loop needs `plan` and `verify` immediately (D-21); `verify` is the single path CI and agents share; the lint trio closes D-22 lockstep, AGENT-02 structure, and D-06 separation (Pitfall 10).
Output: Five skills, three lints with tests, working verify spine.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@product/cli/CLAUDE-CODE-RUNTIME.md
@product/methodology/AI-COWORK.md
@.planning/phases/01-dogfood-bootstrap-enforcement/01-PATTERNS.md
@.planning/spikes/q004-verify-fast.md
@.claude/settings.json
@.cowork/tiers.json
@Taskfile.yml
@specs/005-claude-pack/plan.md
</context>

## Artifacts this phase produces (this plan)

- New file paths: `.claude/skills/{plan,verify,new-module,new-feature,design-implement}/SKILL.md`, `scripts/checks/settings-lint.mjs`, `scripts/checks/skills-lint.mjs`, `scripts/checks/meta-link-lint.mjs`, `scripts/checks/tests/lints.test.mjs`
- New Taskfile tasks: `verify` (full static suite + timing lines), `verify:fast` (alias of the static set per D-19 — identical in Phase 1, diverges when container gates arrive Phase 2)
- New check CLI contracts: `node scripts/checks/settings-lint.mjs` (exit 1 if allow rules missing or deny≠tiers t4); `node scripts/checks/skills-lint.mjs` (exit 1 unless 5 skills with frontmatter name+description); `node scripts/checks/meta-link-lint.mjs` (exit 1 on `.planning/` references inside docs/, specs/, backend/, frontend/, .claude/, templates/, scripts/, infra/)
- New skill frontmatter convention: YAML `name` + `description` (description = trigger surface)

<tasks>

<task type="auto">
  <name>Task 1: plan + verify skills (full, D-21)</name>
  <files>.claude/skills/plan/SKILL.md, .claude/skills/verify/SKILL.md</files>
  <read_first>
    - product/cli/CLAUDE-CODE-RUNTIME.md (§2 skills table — purposes and tier links)
    - product/methodology/AI-COWORK.md (§3.2 plan content, §4 spec format — what the plan skill must emit)
    - .planning/phases/01-dogfood-bootstrap-enforcement/01-PATTERNS.md (skills section — plan/verify contracts)
  </read_first>
  <action>
    Write `.claude/skills/plan/SKILL.md` (full): frontmatter name "plan", description triggering on plan/spec/T3-change intent. Body instructs the agent to: interrogate change intent (what/why, affected areas); compute the next NNN by scanning existing `specs/[0-9][0-9][0-9]-*` dirs (000 reserved); create `specs/NNN-slug/plan.md` in AI-COWORK §3.2 format — files to touch, modules affected, events/SPI surfaces, migrations, tests, `tier: T1|T2|T3` declaration with the §6 tier table as the classification rubric, constitution rules that apply, and an EMPTY `Approved-by:` line the human fills (the skill never fills it — that is the human's H2 act); remind that T3 implementation is hook-blocked until approval.

    Write `.claude/skills/verify/SKILL.md` (full): frontmatter name "verify", description triggering on verify/gates/pre-PR checks. Body: run `task verify` (thin wrapper rule — CI and skill share ONE path, Pattern 7); parse the per-gate timing lines; summarize failures BY VIOLATED RULE with the fix hint each gate already prints (FR-E08 seed); never weaken a gate to get green (AI-COWORK §3.3 — gate changes are T3).
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const p=fs.readFileSync('.claude/skills/plan/SKILL.md','utf8');for(const k of ['name:','description:','tier:','Approved-by','specs/'])if(!p.includes(k))throw 'plan missing: '+k;const v=fs.readFileSync('.claude/skills/verify/SKILL.md','utf8');for(const k of ['name:','description:','task verify'])if(!v.includes(k))throw 'verify missing: '+k;console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - plan/SKILL.md instructs NNN computation from existing dirs, emits the §3.2 field list, and leaves Approved-by empty for the human
    - verify/SKILL.md delegates to `task verify` (no parallel gate list of its own) and mandates rule-named failure summaries
    - Both have YAML frontmatter with non-empty name + description
  </acceptance_criteria>
  <done>The dogfood loop's two operative skills exist in full (D-21); H2 flow is producible in-session.</done>
</task>

<task type="auto">
  <name>Task 2: Three skeleton skills + skills-lint (D-21)</name>
  <files>.claude/skills/new-module/SKILL.md, .claude/skills/new-feature/SKILL.md, .claude/skills/design-implement/SKILL.md, scripts/checks/skills-lint.mjs</files>
  <read_first>
    - product/cli/CLAUDE-CODE-RUNTIME.md (§2 table — exact purposes: new-module scaffolds package-info.java/allowedDependencies/spi+events/Flyway folder/@ApplicationModuleTest/count bump; new-feature zone-correct folder + i18n keys; design-implement Stitch flow [deferred])
  </read_first>
  <action>
    Write the three skeleton SKILL.md files: frontmatter name + description, then a CONTRACT section declaring inputs, outputs, and tier (`new-module` = `tier: T3` with note "skill output includes a pre-filled specs/NNN plan.md template"; `new-feature` = T2; `design-implement` = T2, design workflow currently OFF/deferred). Body states explicitly: "Skeleton — implemented in Phase 2 (new-module) / Phase 4 (new-feature, design-implement). Do NOT improvise an implementation from this contract; report to the human instead." (PATTERNS skills section.)

    Write `scripts/checks/skills-lint.mjs` (zero-dep): assert exactly the five skill dirs exist, each SKILL.md parses YAML frontmatter with non-empty `name` and `description`, `plan`/`verify` bodies exceed a minimal length (full), and the three skeletons contain the literal "Skeleton" and a `tier:` declaration. Exit 1 with rule-named messages on violation.
  </action>
  <verify>
    <automated>node scripts/checks/skills-lint.mjs && node -e "const m=require('fs').readFileSync('.claude/skills/new-module/SKILL.md','utf8');if(!m.includes('tier: T3')||!m.includes('Skeleton'))throw 'new-module contract incomplete';console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/checks/skills-lint.mjs` exits 0 against the real tree; exits 1 when a skill dir is renamed away (negative check during development)
    - new-module declares tier: T3 + pre-filled plan.md template note; all skeletons forbid improvisation
  </acceptance_criteria>
  <done>AGENT-02 structurally complete: 5 skills shipped at the D-21 depth split, machine-checked.</done>
</task>

<task type="auto">
  <name>Task 3: settings-lint + meta-link-lint + task verify/verify:fast spine</name>
  <files>scripts/checks/settings-lint.mjs, scripts/checks/meta-link-lint.mjs, scripts/checks/tests/lints.test.mjs, Taskfile.yml</files>
  <read_first>
    - .planning/spikes/q004-verify-fast.md (the gate-set contract + timing line format `GATE <name> <millis>ms <PASS|FAIL>` — implement exactly)
    - .cowork/tiers.json and .claude/settings.json (the lockstep pair settings-lint compares)
    - Taskfile.yml (current up/down/ps/logs tasks — extend, don't rewrite)
  </read_first>
  <action>
    Write `scripts/checks/settings-lint.mjs` (zero-dep): assert `.claude/settings.json` permissions.allow contains the three T1 rules (Bash(task *), Bash(./mvnw *), Bash(npm run *)) — AGENT-04; assert every tiers.json t4.commandPatterns entry has a corresponding permissions.deny `Bash(...)` rule (lockstep, D-22 anti-duplication rule); assert both PreToolUse hooks and the SessionStart hook are wired. Rule-named failures.

    Write `scripts/checks/meta-link-lint.mjs` (zero-dep): scan shipped dirs (docs/, specs/, backend/, frontend/, .claude/, templates/, scripts/, infra/, .github/) for the string `.planning/` and fail listing file+line on any hit (Pitfall 10, D-06/D-07).

    Write `scripts/checks/tests/lints.test.mjs` (node:test): fixture-based positive/negative cases for both lints (e.g., settings fixture missing a deny rule → exit 1; doc fixture containing `.planning/` → exit 1).

    Extend `Taskfile.yml` with `verify` and `verify:fast` tasks implementing the Q-004 contract: run, in order, `node --test .claude/hooks/tests/ scripts/checks/tests/ scripts/tests/`, `node scripts/checks/claude-md-check.mjs ...` (same args as the CI workflow), `node scripts/checks/settings-lint.mjs`, `node scripts/checks/skills-lint.mjs`, `node scripts/checks/meta-link-lint.mjs`, `docker compose -f infra/compose.yaml config -q` — each wrapped so the task prints `GATE <name> <millis>ms <PASS|FAIL>` per gate and a total line (implement the timing wrapper as a small `scripts/checks/run-gate.mjs` helper if needed — zero-dep). In Phase 1 `verify:fast` and `verify` run the same static set (D-19 — divergence begins when container gates arrive in Phase 2; encode the split as separate task definitions now). Run `task verify` locally, confirm all gates PASS and total <60s; append the measured baseline to `.planning/spikes/q004-verify-fast.md` (replacing the "to be captured" marker).
  </action>
  <verify>
    <automated>node --test scripts/checks/tests/lints.test.mjs && task verify</automated>
  </verify>
  <acceptance_criteria>
    - `task verify` exits 0 printing one `GATE <name> <millis>ms PASS` line per gate plus a total; total <60s
    - `task verify:fast` exists as a distinct task (static set per D-19)
    - settings-lint fails (exit 1) when a t4 pattern is removed from permissions.deny in a fixture copy (lockstep proven by test)
    - meta-link-lint passes on the real tree (zero .planning/ references in shipped dirs) and fails on the fixture
    - Q-004 report updated with the measured Phase-1 baseline
  </acceptance_criteria>
  <done>One verify path shared by CI, agents, and devs, with rule-named legible failures and the D-19 fast/full seam in place; D-06 separation machine-enforced.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Skill instructions → agent behavior | Skills steer the agent; a tampered skill is an instruction-injection vector |
| settings.json deny rules ↔ tiers.json | Drift between the mirror and the source silently weakens T4 defense-in-depth |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-33 | Tampering | Skill files edited to bypass methodology | mitigate | .claude/** under CODEOWNERS; settings/skills are T3-adjacent paths; skills-lint asserts structure every verify run |
| T-01-34 | Elevation | deny↔tiers drift reopening T4 commands | mitigate | settings-lint lockstep check in verify + CI, fixture-tested |
| T-01-35 | Information disclosure | Shipped artifact leaking .planning/ internals | mitigate | meta-link-lint fails on any reference (Pitfall 10) |
| T-01-SC | Tampering | package installs | accept | Zero npm dependencies |
</threat_model>

<verification>
- `task verify` green end-to-end with timing lines; lints fixture tests green
- skills-lint green against the real 5-skill tree
</verification>

<success_criteria>
- AGENT-02 (five skills, D-21 depth), AGENT-04 lockstep proof, D-06 separation enforced
- Q-004 contract implemented with measured Phase-1 baseline <60s
</success_criteria>

<output>
Create `.planning/phases/01-dogfood-bootstrap-enforcement/01-11-SUMMARY.md` when done.
</output>
