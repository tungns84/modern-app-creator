# {{projectName}}

Root constitution fixture for the GATE-12 render-then-check pipeline tests.

## Coordinates

- groupId: {{groupId}}
- artifactId: {{artifactId}}

## Canonical commands

- `task up`
- `./mvnw verify`
- `npm run dev`

This fixture stays well under both the 200-line root budget and the
150-line tree-file budget so it can serve as a passing case for either.
