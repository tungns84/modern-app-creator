---
phase: 01-dogfood-bootstrap-enforcement
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .gitattributes
  - .gitignore
  - .cowork/tiers.json
  - .cowork/waivers.json
  - docs/methodology/AI-COWORK.md
  - .github/pull_request_template.md
  - specs/001-hooks-enforcement/spec.md
  - specs/001-hooks-enforcement/plan.md
  - specs/002-plan-compliance-ci/spec.md
  - specs/002-plan-compliance-ci/plan.md
  - specs/003-local-stack/spec.md
  - specs/003-local-stack/plan.md
  - specs/004-init-script/spec.md
  - specs/004-init-script/plan.md
  - specs/005-claude-pack/spec.md
  - specs/005-claude-pack/plan.md
  - specs/006-adr-foundation/spec.md
  - specs/006-adr-foundation/plan.md
autonomous: true
requirements: [AGENT-05, AGENT-06, AGENT-09]
must_haves:
  truths:
    - "Tracked text files are LF-normalized via .gitattributes (CRLF cannot poison line counts, parity diffs, or hook shebangs)"
    - "A single config file (.cowork/tiers.json) holds T3 path globs, T4 command patterns, meta-dir exclusions, and the pinned Claude Code version"
    - "Six hand-written spec units (specs/001..006) exist with spec.md + plan.md, tier: T3, and an Approved-by audit line — manual dogfood stage of the ratchet (D-05)"
    - "The waiver register (.cowork/waivers.json) carries a time-boxed self-approval waiver (D-10)"
    - "The methodology constitution ships at docs/methodology/AI-COWORK.md and the PR template carries the H3 checklist"
  artifacts:
    - path: ".gitattributes"
      provides: "LF normalization (* text=auto eol=lf) with CRLF exceptions for *.cmd/*.bat/*.ps1"
    - path: ".cowork/tiers.json"
      provides: "Single source of truth for tiers (D-22)"
      contains: "testedVersion"
    - path: ".cowork/waivers.json"
      provides: "Time-boxed waiver register (D-10)"
      contains: "self-approval"
    - path: "specs/001-hooks-enforcement/plan.md"
      provides: "First manual dogfood spec unit (D-05/D-09)"
      contains: "Approved-by:"
    - path: "docs/methodology/AI-COWORK.md"
      provides: "Constitution copy (ships in preset, D-12)"
  key_links:
    - from: ".cowork/tiers.json"
      to: ".claude/hooks/lib/tiers.mjs (plan 01-04) and scripts/checks/plan-compliance.mjs (plan 01-09)"
      via: "single shared tier config consumed by both enforcement layers"
      pattern: "tiers\\.json"
    - from: "specs/00*/plan.md"
      to: "plan-compliance CI gate (plan 01-09) and T3 hook (plan 01-04)"
      via: "Approved-by audit line + tier declaration"
      pattern: "Approved-by:"
---

<objective>
Bootstrap the repo conventions every other Phase 1 plan stands on: LF normalization first commit (Pitfall 6), the single tier source of truth (D-22), the waiver register (D-10), the GitHub remote (D-01), the specs/NNN convention with six hand-written spec units (D-05, D-06, D-09), the shipped methodology constitution copy, and the H3 PR template. This is the "manual dogfood" stage of the enforcement ratchet (AGENT-09): specs exist and are approved BEFORE any automation enforces them.

Purpose: Without .gitattributes the init-parity diff, CLAUDE.md line budgets, and hook scripts all break on Windows (core.autocrlf=true on this machine). Without tiers.json + specs the hooks (01-04) and CI gate (01-09) have nothing to read.
Output: Root repo layout per D-12 — config + spec + doc artifacts only, no application code.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-dogfood-bootstrap-enforcement/01-CONTEXT.md
@.planning/phases/01-dogfood-bootstrap-enforcement/01-RESEARCH.md
@.planning/phases/01-dogfood-bootstrap-enforcement/01-PATTERNS.md
@product/methodology/AI-COWORK.md
</context>

## Artifacts this phase produces (this plan)

- New file paths: `.gitattributes`, `.gitignore`, `.cowork/tiers.json`, `.cowork/waivers.json`, `docs/methodology/AI-COWORK.md`, `.github/pull_request_template.md`, `specs/001-hooks-enforcement/{spec,plan}.md`, `specs/002-plan-compliance-ci/{spec,plan}.md`, `specs/003-local-stack/{spec,plan}.md`, `specs/004-init-script/{spec,plan}.md`, `specs/005-claude-pack/{spec,plan}.md`, `specs/006-adr-foundation/{spec,plan}.md`
- New config keys (`.cowork/tiers.json`): `claudeCode.testedVersion`, `meta.exclude[]`, `t3.paths[]`, `t4.commandPatterns[]`
- New config keys (`.cowork/waivers.json`): `waivers[].{id,scope,reason,approvedBy,created,expires}` — first entry id `W-001`, scope `self-approval`
- New conventions: spec dir naming `specs/NNN-short-name/`, branch naming `feat/NNN-short-name`, commit REQ-ID citation rule

<tasks>

<task type="auto">
  <name>Task 1: First-commit safety net — .gitattributes, .gitignore, GitHub remote, tiers + waivers config</name>
  <files>.gitattributes, .gitignore, .cowork/tiers.json, .cowork/waivers.json</files>
  <read_first>
    - .planning/phases/01-dogfood-bootstrap-enforcement/01-RESEARCH.md (Pitfall 6 lines 403-406; Pattern 2 line 301; architecture map line 119)
    - .planning/phases/01-dogfood-bootstrap-enforcement/01-CONTEXT.md (D-01, D-10, D-22, Claude's Discretion meta-dir list)
    - product/methodology/AI-COWORK.md (§5 Layer 2 T3 path list, §6 tier table)
  </read_first>
  <action>
    Create `.gitattributes` with: `* text=auto eol=lf` plus CRLF exceptions `*.cmd text eol=crlf`, `*.bat text eol=crlf`, `*.ps1 text eol=crlf`, and binary markers for `*.png`, `*.svg`, `*.jar` (`binary`). Run `git add --renormalize .` so existing tracked files re-stage with LF (this machine has core.autocrlf=true — Pitfall 6). Commit `.gitattributes` + renormalization as the FIRST commit of this plan.

    Create `.gitignore` (or extend if present) with: `.claude/settings.local.json`, `node_modules/`, `.task/`, OS noise (`Thumbs.db`, `.DS_Store`).

    Ensure GitHub remote exists per D-01: run `git remote get-url origin`; if missing, `gh repo create` (private, current account) and push `master`. Record the resulting `owner/repo` slug in the SUMMARY (plans 01-02/01-03/01-09 need it).

    Create `.cowork/tiers.json` — the single tier source of truth (D-22), JSON object with exactly these top-level keys:
    - `claudeCode.testedVersion`: empty string "" (filled by Q-010 spike in plan 01-03; consumed by session-version-warn per D-11)
    - `meta.exclude`: [".planning/**", "product/**", "requirements/**", ".code-review-graph/**", "CLAUDE.md"] — root dev CLAUDE.md is GSD-owned (D-13); meta dirs are never T3 (CONTEXT discretion: GSD meta-commits need no spec)
    - `t3.paths`: full AI-COWORK §5 L2 list from day 1 (not-yet-existing paths simply never match): "pom.xml", "**/pom.xml", "package.json", "**/package.json", "backend/src/main/java/**/security/**", "backend/src/main/java/**/tenancy/**", "backend/src/main/java/**/package-info.java", ".github/**", ".cowork/**", ".claude/settings.json", ".claude/hooks/**", "backend/CLAUDE.md", "frontend/CLAUDE.md", "templates/claude/**", "docs/adr/**", "docs/methodology/**", "scripts/checks/**", "Taskfile.yml", "infra/compose.yaml"
    - `t4.commandPatterns`: ["git push --force", "git push -f", "kubectl apply", "kubectl delete", "flyway clean", "docker volume rm", "gh secret set", "git reset --hard origin"]
    - `strict.t2Paths`: [] — scaffold seam only, exercised later (deferred idea, ships as empty config)

    Create `.cowork/waivers.json` with key `waivers` (array). First entry per D-10: id "W-001", scope "self-approval", reason "solo developer — second reviewer does not exist yet", approvedBy the GitHub login of the developer, created today's date, expires "2026-09-30". Both .cowork files are themselves T3 (gate config).
  </action>
  <verify>
    <automated>node -e "const t=require('./.cowork/tiers.json');const w=require('./.cowork/waivers.json');if(!Array.isArray(t.t3.paths)||t.t3.paths.length<15)throw 't3 list short';if(!t.meta.exclude.includes('.planning/**'))throw 'meta exclude missing';if(!Array.isArray(t.t4.commandPatterns)||t.t4.commandPatterns.length<5)throw 't4 short';if(w.waivers[0].scope!=='self-approval')throw 'waiver missing';console.log('OK')" && git check-attr text -- Taskfile.yml | findstr "auto" && git remote get-url origin</automated>
  </verify>
  <acceptance_criteria>
    - `.gitattributes` first line is `* text=auto eol=lf`; contains `*.ps1 text eol=crlf`
    - `node -e` assertion above prints OK (tiers.json has ≥15 t3 globs incl. ".cowork/**" and ".claude/hooks/**"; meta.exclude includes ".planning/**" and "CLAUDE.md"; waivers[0].scope == "self-approval" with non-empty expires)
    - `git remote get-url origin` returns a github.com URL
    - `git status` shows no unstaged renormalization leftovers after commit
  </acceptance_criteria>
  <done>Repo is LF-safe, remoted on GitHub, and both enforcement layers have their single config source (D-22) plus the waiver register (D-10).</done>
</task>

<task type="auto">
  <name>Task 2: Hand-written spec units 001–006 (manual dogfood stage, D-05/D-09)</name>
  <files>specs/001-hooks-enforcement/spec.md, specs/001-hooks-enforcement/plan.md, specs/002-plan-compliance-ci/spec.md, specs/002-plan-compliance-ci/plan.md, specs/003-local-stack/spec.md, specs/003-local-stack/plan.md, specs/004-init-script/spec.md, specs/004-init-script/plan.md, specs/005-claude-pack/spec.md, specs/005-claude-pack/plan.md, specs/006-adr-foundation/spec.md, specs/006-adr-foundation/plan.md</files>
  <read_first>
    - product/methodology/AI-COWORK.md (§3.2 plan content, §4 spec artifact format — the authoritative format)
    - .planning/phases/01-dogfood-bootstrap-enforcement/01-PATTERNS.md (specs/NNN section — format excerpt and ratchet narrative)
    - .planning/phases/01-dogfood-bootstrap-enforcement/01-CONTEXT.md (D-05, D-09, D-10)
  </read_first>
  <action>
    Write six spec units in English (technical artifacts), numbering from 001 (`000-example` reserved for Phase 8 per D-09). Grouping adjusts D-09's five suggested units by adding 006 for the ADR set (planner-permitted grouping adjustment):
    - `001-hooks-enforcement` — covers `.cowork/tiers.json`, `.cowork/waivers.json`, `.claude/hooks/**`, `.claude/settings.json` (REQ: AGENT-03, AGENT-04)
    - `002-plan-compliance-ci` — covers `.github/workflows/plan-compliance.yml`, `.github/CODEOWNERS`, `scripts/checks/plan-compliance.mjs`, `scripts/checks/assert-non-author-approval.mjs`, repo ruleset/branch-protection config (REQ: GATE-10)
    - `003-local-stack` — covers `infra/compose.yaml`, `Taskfile.yml`, `.github/workflows/os-matrix.yml` (REQ: FOUND-02)
    - `004-init-script` — covers `scripts/init.sh`, `scripts/init.ps1`, `scripts/init-core.mjs`, `.github/workflows/init-parity.yml` (REQ: FOUND-12)
    - `005-claude-pack` — covers `templates/claude/**`, `backend/CLAUDE.md`, `frontend/CLAUDE.md`, `.claude/skills/**`, `scripts/checks/{render-claude-md,claude-md-check,settings-lint,skills-lint,meta-link-lint}.mjs`, `.github/workflows/claude-md-check.yml` (REQ: AGENT-01, AGENT-02, GATE-12)
    - `006-adr-foundation` — covers `docs/adr/0001..0003` (constitution change = T3 per AI-COWORK §2)

    Each `spec.md`: user story, acceptance criteria (lift from the matching plan's must_haves in this phase), REQ-IDs cited. Each `plan.md`: files to touch, modules affected (n/a pre-backend), tests, `tier: T3`, and the audit line `Approved-by: <github-login> 2026-06-11 (H2 approval granted via GSD Phase-1 plan review; solo self-approval waived per .cowork/waivers.json W-001)`. Per D-02 this line is audit trail, NOT authorization proof — state that sentence verbatim inside each plan.md.

    Specs must NOT link into `.planning/` (D-06/D-07 — they ship in the template; meta-link-lint in plan 01-11 will enforce this).
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const dirs=['001-hooks-enforcement','002-plan-compliance-ci','003-local-stack','004-init-script','005-claude-pack','006-adr-foundation'];for(const d of dirs){const p=fs.readFileSync('specs/'+d+'/plan.md','utf8');if(!/tier:\s*T3/.test(p))throw d+' missing tier';if(!/Approved-by:\s*\S/.test(p))throw d+' missing approval';if(/\.planning\//.test(p))throw d+' links .planning';fs.accessSync('specs/'+d+'/spec.md')};console.log('OK 6 specs')"</automated>
    <human-check>End-of-phase: developer confirms the six Approved-by lines reflect their real H2 approval of this phase plan set (dogfood integrity, D-05/D-10).</human-check>
  </verify>
  <acceptance_criteria>
    - 6 directories `specs/00[1-6]-*/` each contain `spec.md` and `plan.md`
    - Every plan.md contains the literal `tier: T3`, a non-empty `Approved-by:` line, and zero occurrences of the string `.planning/`
    - Every spec.md cites at least one REQ-ID from {AGENT-01..06, AGENT-09, GATE-10, GATE-12, FOUND-02, FOUND-12}
  </acceptance_criteria>
  <done>The manual-dogfood stage of the ratchet is on record: every T3 deliverable of Phase 1 has an approved spec unit BEFORE hooks or CI enforce anything (D-05; AGENT-06; AGENT-09 evidence).</done>
</task>

<task type="auto">
  <name>Task 3: Ship the constitution copy and the H3 PR template</name>
  <files>docs/methodology/AI-COWORK.md, .github/pull_request_template.md</files>
  <read_first>
    - product/methodology/AI-COWORK.md (the verbatim source; §11 bootstrap checklist defines PR-template content)
  </read_first>
  <action>
    Copy `product/methodology/AI-COWORK.md` byte-for-byte to `docs/methodology/AI-COWORK.md` (D-12: docs/ ships in the preset; product/ is meta and never ships). Do not edit content — verbatim copy, verified by diff.

    Create `.github/pull_request_template.md` with the H3 checklist per AI-COWORK §11: link to `specs/NNN-*/` dir, declared tier (T1/T2/T3/T4), checklist items — "spec linked", "tier declared", "plan approved (T3)", "gates green locally (`task verify`)", "UI manual checks done (if UI)", "spec reconciled in this PR if behavior diverged (§4 spec-rot rule)". Keep under 40 lines.
  </action>
  <verify>
    <automated>git diff --no-index --quiet product/methodology/AI-COWORK.md docs/methodology/AI-COWORK.md && node -e "const t=require('fs').readFileSync('.github/pull_request_template.md','utf8');if(!/specs\//.test(t)||!/[Tt]ier/.test(t))throw 'template incomplete';console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `git diff --no-index` between product/ source and docs/ copy exits 0 (byte-identical)
    - PR template references `specs/` and tier declaration; ≤40 lines
  </acceptance_criteria>
  <done>Constitution ships at docs/methodology/AI-COWORK.md (AI-COWORK §11 last checkbox) and every PR carries the H3 checklist (AGENT-05).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| tiers.json → both enforcement layers | A corrupted/weakened tier list silently disables T3 gating everywhere (D-22 single source = single point of tampering) |
| waivers.json → CI gate verdict | An over-broad or never-expiring waiver becomes a permanent bypass |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | .cowork/tiers.json | mitigate | tiers.json itself listed in its own t3.paths (".cowork/**") — edits require approved plan once gates are live; CODEOWNERS covers .cowork/** (plan 01-09) |
| T-01-02 | Repudiation/Elevation | .cowork/waivers.json | mitigate | Waiver entries carry mandatory `expires`; CI gate (plan 01-09) fails on expired waiver; register is T3-pathed |
| T-01-03 | Spoofing | Approved-by line in specs/*/plan.md | accept (by design) | D-02: line is audit-only; authorization is PR-review API + ruleset — sentence recorded verbatim in each plan.md |
| T-01-SC | Tampering | package installs | accept | No npm/pip/cargo installs in this plan (Phase 1 is zero-dependency per RESEARCH Package Legitimacy Audit) |
</threat_model>

<verification>
- `node -e` assertions for tiers.json/waivers.json shape, 6 spec units, PR template pass
- `git log --oneline` shows .gitattributes + renormalization as the first commit of the plan (ratchet evidence ordering)
- `git diff --no-index` proves docs/methodology copy is verbatim
</verification>

<success_criteria>
- LF normalization active before any script/check lands (Pitfall 6 closed)
- Single tier source (D-22) and waiver register (D-10) exist and parse
- specs/001..006 approved units on record before hooks/CI go live (D-05, AGENT-06, AGENT-09)
- Constitution copy + H3 PR template shipped (AGENT-05)
</success_criteria>

<output>
Create `.planning/phases/01-dogfood-bootstrap-enforcement/01-01-SUMMARY.md` when done. Record the GitHub repo slug for downstream plans.
</output>
