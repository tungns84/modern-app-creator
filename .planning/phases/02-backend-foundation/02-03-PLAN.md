---
phase: 02-backend-foundation
plan: 03
type: execute
wave: 3
depends_on: ["02-02"]
files_modified:
  - backend/src/main/java/com/acme/app/i18n/package-info.java
  - backend/src/main/java/com/acme/app/i18n/spi/package-info.java
  - backend/src/main/java/com/acme/app/i18n/spi/MessageResolver.java
  - backend/src/main/java/com/acme/app/i18n/DefaultMessageResolver.java
  - backend/src/main/resources/i18n/messages_vi.properties
  - backend/src/main/resources/i18n/messages_en.properties
  - backend/src/main/java/com/acme/app/caching/package-info.java
  - backend/src/main/java/com/acme/app/caching/spi/package-info.java
  - backend/src/main/java/com/acme/app/caching/spi/AppCache.java
  - backend/src/main/java/com/acme/app/caching/CaffeineCacheConfig.java
  - backend/src/test/java/com/acme/app/i18n/I18nModuleTest.java
  - backend/src/test/java/com/acme/app/caching/CachingModuleTest.java
  - backend/src/test/java/com/acme/app/ModulithVerifyTest.java
  - specs/010-i18n-module/spec.md
  - specs/010-i18n-module/plan.md
  - specs/011-caching-module/spec.md
  - specs/011-caching-module/plan.md
autonomous: false
requirements: [FOUND-01, GATE-01, GATE-02]

must_haves:
  truths:
    - "`i18n` and `caching` modules were each generated WITH the new-module skill (dogfood runs 2 and 3 of 4)"
    - "`i18n` resolves vi/en messages via its `::spi` surface, reading appconfig::spi typed properties only"
    - "`caching` provides a Caffeine-backed cache abstraction via its `::spi` surface"
    - "Both modules read config only via @ConfigurationProperties records (no @Value/Environment) — GATE-02 D-09 stays green"
    - "Modulith verify passes with the name set {shared, appconfig, i18n, caching} and a DAG cycle would fail it"
  artifacts:
    - path: "backend/src/main/java/com/acme/app/i18n/package-info.java"
      provides: "i18n module boundary allowedDependencies = {shared, appconfig::spi}"
      contains: "@ApplicationModule"
    - path: "backend/src/main/java/com/acme/app/i18n/spi/MessageResolver.java"
      provides: "vi/en message resolution SPI"
    - path: "backend/src/main/resources/i18n/messages_vi.properties"
      provides: "Vietnamese message bundle"
    - path: "backend/src/main/resources/i18n/messages_en.properties"
      provides: "English message bundle"
    - path: "backend/src/main/java/com/acme/app/caching/package-info.java"
      provides: "caching module boundary allowedDependencies = {shared, appconfig::spi}"
      contains: "@ApplicationModule"
    - path: "backend/src/main/java/com/acme/app/caching/spi/AppCache.java"
      provides: "Caffeine cache abstraction SPI"
  key_links:
    - from: "backend/src/main/java/com/acme/app/i18n/DefaultMessageResolver.java"
      to: "backend/src/main/resources/i18n/messages_vi.properties"
      via: "MessageSource bundle lookup for vi + en"
      pattern: "messages"
    - from: "backend/src/main/java/com/acme/app/caching/CaffeineCacheConfig.java"
      to: "backend/src/main/java/com/acme/app/appconfig/spi/package-info.java"
      via: "injects appconfig::spi typed properties for cache TTL/size"
      pattern: "appconfig"
---

<objective>
Dogfood the `new-module` skill two more times (runs 2 and 3 of 4) by generating `i18n` and `caching`, each a minimal REAL functional slice (D-04). `i18n` resolves vi/en messages through its `::spi` surface; `caching` provides a Caffeine-backed cache abstraction through its `::spi` surface. Both depend only on `shared` and `appconfig::spi`, and both read configuration exclusively via `@ConfigurationProperties` records from `appconfig::spi` (no `@Value`/`Environment` — GATE-02 D-09).

Purpose: These two leaf-ish infra modules prove the skill scales to multiple module shapes and keep the architecture gates exercised against a growing DAG. They are independent of each other and of observability (Plan 04), but both edit the shared `ModulithVerifyTest` name set, so this plan owns that edit in its own wave.
Output: `i18n` and `caching` modules with `::spi` surfaces, vi/en bundles, Caffeine config; name set bumped to `{shared, appconfig, i18n, caching}`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/02-backend-foundation/02-CONTEXT.md
@.planning/phases/02-backend-foundation/02-RESEARCH.md
@.planning/phases/02-backend-foundation/02-PATTERNS.md
@backend/CLAUDE.md
@.claude/skills/new-module/SKILL.md
@.claude/skills/plan/SKILL.md
@.cowork/tiers.json
@docs/adr/0001-valkey-not-redis.md
@.planning/phases/02-backend-foundation/02-02-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Generate i18n module (vi/en message resolution via ::spi)</name>
  <files>backend/src/main/java/com/acme/app/i18n/package-info.java, backend/src/main/java/com/acme/app/i18n/spi/package-info.java, backend/src/main/java/com/acme/app/i18n/spi/MessageResolver.java, backend/src/main/java/com/acme/app/i18n/DefaultMessageResolver.java, backend/src/main/resources/i18n/messages_vi.properties, backend/src/main/resources/i18n/messages_en.properties, backend/src/test/java/com/acme/app/i18n/I18nModuleTest.java, specs/010-i18n-module/spec.md, specs/010-i18n-module/plan.md</files>
  <read_first>
    - .claude/skills/new-module/SKILL.md (the implemented generator — run it, do not improvise layout)
    - .planning/phases/02-backend-foundation/02-RESEARCH.md (Pattern 1 module boundary with appconfig::spi; Pattern 3 @ApplicationModuleTest; D-04 i18n = vi/en resolution)
    - .planning/phases/02-backend-foundation/02-PATTERNS.md (package-info boundary pattern; @ConfigurationProperties consumption pattern; module communication rules)
    - .planning/phases/02-backend-foundation/02-02-SUMMARY.md (appconfig::spi surface — what records are available)
    - backend/src/main/java/com/acme/app/appconfig/spi/package-info.java (the SPI this module reads)
    - .claude/skills/plan/SKILL.md (bound-spec format)
  </read_first>
  <behavior>
    - `MessageResolver.resolve("greeting", Locale.forLanguageTag("vi"))` returns the Vietnamese string; the same key under `en` returns the English string.
    - A missing key resolves predictably (returns the key or a documented default, asserted by the test) — no exception leak.
    - Resolution config (default locale, supported locales) comes from an appconfig::spi `@ConfigurationProperties` record — no `@Value` in i18n.
  </behavior>
  <action>
    Author bound spec `specs/010-i18n-module/{spec,plan}.md` (tier T3 — package-info is T3; `Approved-by:` EMPTY); bind branch `feat/010-i18n-module`. Run the `new-module` scaffold for `i18n` with allowedDependencies={shared, "appconfig::spi"}, exposesSpi=true, exposesEvents=false, hasTables=false (no i18n tables — D-04, no empty Flyway folder). Fill the module: `i18n/spi/package-info.java` (`@NamedInterface("spi")`), `i18n/spi/MessageResolver.java` (interface: `String resolve(String key, Locale locale)` and `Set<Locale> supportedLocales()`), `i18n/DefaultMessageResolver.java` (constructor-injected Spring `MessageSource` over the `i18n/messages` basename + the i18n config record from appconfig::spi; constructor injection only). Create `messages_vi.properties` and `messages_en.properties` with a small seed key set (e.g. a `greeting` and a `validation.invalid` key) present in BOTH bundles (the Phase 4 i18n parity gate will later scan these — keep keys identical across files). Write `I18nModuleTest` (`@ApplicationModuleTest`) asserting vi and en resolution and the missing-key behavior. Update `ModulithVerifyTest.BASE_MODULES` to include `i18n` (the scaffold bumps this; this plan also adds `caching` in Task 2 — make ONE coherent edit covering both).
  </action>
  <verify>
    <automated>./mvnw -q test -Dtest=I18nModuleTest -f backend/pom.xml</automated>
  </verify>
  <acceptance_criteria>
    - `I18nModuleTest` passes: vi and en keys resolve to distinct localized strings; missing-key behavior is asserted.
    - `i18n/package-info.java` declares `@ApplicationModule(allowedDependencies = {"shared", "appconfig::spi"})`; `i18n/spi` declares `@NamedInterface("spi")`.
    - `messages_vi.properties` and `messages_en.properties` carry an identical key set.
    - No `@Value`/`Environment` in the i18n package (GATE-02 D-09 green).
  </acceptance_criteria>
  <done>i18n resolves vi/en messages via ::spi; dogfood run 2/4 of the skill complete.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Generate caching module (Caffeine cache abstraction via ::spi) + name-set bump</name>
  <files>backend/src/main/java/com/acme/app/caching/package-info.java, backend/src/main/java/com/acme/app/caching/spi/package-info.java, backend/src/main/java/com/acme/app/caching/spi/AppCache.java, backend/src/main/java/com/acme/app/caching/CaffeineCacheConfig.java, backend/src/test/java/com/acme/app/caching/CachingModuleTest.java, backend/src/test/java/com/acme/app/ModulithVerifyTest.java, specs/011-caching-module/spec.md, specs/011-caching-module/plan.md</files>
  <read_first>
    - .claude/skills/new-module/SKILL.md (run the generator)
    - .planning/phases/02-backend-foundation/02-RESEARCH.md (D-04 caching = Caffeine + cache abstraction; Don't Hand-Roll table; Pattern 3)
    - .planning/phases/02-backend-foundation/02-PATTERNS.md (@ConfigurationProperties consumption — cache TTL from appconfig::spi, NOT @Value; constructor injection pattern)
    - docs/adr/0001-valkey-not-redis.md (cache infra choice — Caffeine in dev; Valkey is the prod container, not wired here)
    - backend/src/main/java/com/acme/app/appconfig/spi/package-info.java (typed properties for cache config)
    - CLAUDE.md (Caffeine 3.2.4 Boot-managed)
    - .claude/skills/plan/SKILL.md (bound-spec format)
  </read_first>
  <behavior>
    - `AppCache.get(key, loader)` returns a cached value on the second call without re-invoking the loader (Caffeine hit), within the configured TTL/max-size.
    - Cache TTL and max size are read from an appconfig::spi `@ConfigurationProperties` record (no `@Value` in caching).
    - The cache abstraction is exposed through `caching::spi` so other modules depend on the interface, not Caffeine directly.
  </behavior>
  <action>
    Author bound spec `specs/011-caching-module/{spec,plan}.md` (tier T3; `Approved-by:` EMPTY); bind branch `feat/011-caching-module`. Run the `new-module` scaffold for `caching` with allowedDependencies={shared, "appconfig::spi"}, exposesSpi=true, exposesEvents=false, hasTables=false. Fill the module: `caching/spi/package-info.java` (`@NamedInterface("spi")`), `caching/spi/AppCache.java` (interface abstraction: `<T> T get(String name, String key, java.util.function.Supplier<T> loader)` and `void evict(String name, String key)`), `caching/CaffeineCacheConfig.java` (a `@Configuration` building a Caffeine-backed `CacheManager` whose TTL/max-size come from an appconfig::spi typed-properties record — constructor injection only; Caffeine version is Boot-managed). Do NOT wire Valkey/Redis here (ADR-0001 keeps Caffeine for dev; prod cache container is out of Phase 2 scope). Write `CachingModuleTest` (`@ApplicationModuleTest`) asserting a second `get` with the same key skips the loader (cache hit). Finalize the single `ModulithVerifyTest.BASE_MODULES` edit so it now equals `{shared, appconfig, i18n, caching}` (covering both Task 1 and Task 2) and `modules.verify()` passes.
  </action>
  <verify>
    <automated>./mvnw -q test -Dtest=CachingModuleTest,I18nModuleTest,ModulithVerifyTest,ArchitectureGatesTest -f backend/pom.xml</automated>
  </verify>
  <acceptance_criteria>
    - `CachingModuleTest` passes: a repeated `get` is served from Caffeine without re-invoking the loader.
    - `caching/package-info.java` declares `@ApplicationModule(allowedDependencies = {"shared", "appconfig::spi"})`; `caching/spi` declares `@NamedInterface("spi")`.
    - Cache config (TTL, max size) is read from an appconfig::spi record — no `@Value` in caching (GATE-02 D-09 green).
    - `ModulithVerifyTest` asserts the name set `{shared, appconfig, i18n, caching}` (+`bpm` when enabled) and `modules.verify()` passes.
  </acceptance_criteria>
  <done>caching provides a Caffeine cache abstraction via ::spi; dogfood run 3/4 complete; the DAG now has four green modules.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build → main branch | two new module boundaries (package-info) are T3 — bound specs + H2 required |
| cache contents | cached values held in-process (Caffeine) — no external surface in Phase 2 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-07 | Information disclosure | PII or secrets cached in the Caffeine layer | accept | Phase 2 caches only infra/config values; no user/PII data exists until Phase 3+; revisit when a feature module first caches user data (note for Phase 5) |
| T-02-08 | Tampering | i18n message bundle injection (untrusted translation content rendered) | accept | Bundles are repo-controlled static resources, not user input; output-encoding is a Phase 4 frontend/RFC-9457 concern — no untrusted message source in Phase 2 |
| T-02-09 | Tampering | Module boundary widened (allowedDependencies bypass) to take a shortcut dependency | mitigate | `@ApplicationModuleTest` + GATE-01 fail on undeclared deps; package-info is T3 so any boundary widening needs an approved bound spec |
| T-02-SC | Tampering | Maven/npm installs (Caffeine is Boot-managed; no new deps) | mitigate | No new external packages added; Caffeine version comes from the Boot BOM; bound spec reviewed at H2 |
</threat_model>

<verification>
- `./mvnw -q test -Dtest=CachingModuleTest,I18nModuleTest,ModulithVerifyTest,ArchitectureGatesTest -f backend/pom.xml` passes.
- `node scripts/checks/run-gate.mjs --mode fast` green and under the 60s budget with the four-module DAG.
</verification>

<success_criteria>
- `i18n` (vi/en via ::spi) and `caching` (Caffeine via ::spi) are real functional slices (D-04), each generated WITH the `new-module` skill (runs 2 and 3 of 4).
- Both read config only via appconfig::spi `@ConfigurationProperties` records (D-09).
- GATE-01 name set = `{shared, appconfig, i18n, caching}`; GATE-02 stays green.
</success_criteria>

<artifacts_this_phase_produces>
- Modules: `com.acme.app.i18n` (`allowedDependencies = {"shared", "appconfig::spi"}`) + `i18n.spi` (`@NamedInterface("spi")`); `com.acme.app.caching` (same allowedDependencies) + `caching.spi` (`@NamedInterface("spi")`).
- SPI types: `i18n.spi.MessageResolver`; `caching.spi.AppCache`.
- Impl: `i18n.DefaultMessageResolver` (MessageSource over `i18n/messages`); `caching.CaffeineCacheConfig` (Caffeine CacheManager).
- Resources: `i18n/messages_vi.properties`, `i18n/messages_en.properties` (identical key sets).
- Tests: `I18nModuleTest`, `CachingModuleTest` (`@ApplicationModuleTest`).
- Test update: `ModulithVerifyTest.BASE_MODULES` → `{shared, appconfig, i18n, caching}`.
- Specs: `specs/010-i18n-module/{spec,plan}.md`, `specs/011-caching-module/{spec,plan}.md`.
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/02-backend-foundation/02-03-SUMMARY.md` when done.
</output>
