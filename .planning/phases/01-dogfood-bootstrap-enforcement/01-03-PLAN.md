---
phase: 01-dogfood-bootstrap-enforcement
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/spikes/q010-hook-matrix.md
  - .planning/spikes/q010-harness/**
  - .planning/spikes/jdk25-toolchain.md
autonomous: true
requirements: [AGENT-03]
must_haves:
  truths:
    - "Q-010 report records a per-scenario hook behavior matrix on the pinned Claude Code version (Write deny, Edit deny, Bash T4 deny, Bash write-bypass, crash fail-mode, skip-permissions flag, settings.local override, $CLAUDE_PROJECT_DIR expansion, SessionStart warn visibility)"
    - "Assumptions A1–A4 and A9 each have an explicit verdict; untested OS legs are labeled, not implied"
    - "The pinned Claude Code version (from `claude --version`) is recorded for .cowork/tiers.json claudeCode.testedVersion"
    - "JDK 25 toolchain smoke (compiler release 25 + JUnit + Mockito + MapStruct 1.6.3 + JaCoCo 0.8.15 + ArchUnit ≥1.4.2) ran green on the 3-OS GitHub matrix, results in the report"
  artifacts:
    - path: ".planning/spikes/q010-hook-matrix.md"
      provides: "Hook stability matrix gating D-08 hooks-go-live"
      contains: "permissionDecision"
    - path: ".planning/spikes/jdk25-toolchain.md"
      provides: "JDK 25 empirical toolchain confirmation (MapStruct/JaCoCo MEDIUM-confidence items resolved)"
      contains: "temurin"
  key_links:
    - from: ".planning/spikes/q010-hook-matrix.md"
      to: ".claude/hooks/*.mjs and .claude/settings.json (plan 01-04)"
      via: "matrix verdicts gate D-08 go-live and fix the fail-closed design"
      pattern: "fail.?closed"
---

<objective>
Run the remaining two upfront spikes (D-20): Q-010 (Claude Code hook stability matrix on the pinned version — the gate for D-08 hooks-go-live and the empirical test of fail-open/fail-closed semantics) and the JDK 25 toolchain smoke on the 3-OS CI matrix. Reports land in `.planning/spikes/` (meta, D-07).

Purpose: Plan 01-04 turns hooks on for this repo immediately after Q-010 passes (D-08). Assumptions A1–A4/A9 (exit-code fail-open, $CLAUDE_PROJECT_DIR on Windows, skip-permissions behavior, settings.local merge semantics, SessionStart stdout visibility) must be observed, not believed. The JDK smoke empirically settles MapStruct/JaCoCo on 25 before Phase 2 builds a real backend.
Output: Two spike reports + reusable Q-010 scenario harness (meta).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-dogfood-bootstrap-enforcement/01-CONTEXT.md
@.planning/phases/01-dogfood-bootstrap-enforcement/01-RESEARCH.md
@.planning/phases/01-dogfood-bootstrap-enforcement/01-PATTERNS.md
</context>

## Artifacts this phase produces (this plan)

- New file paths: `.planning/spikes/q010-hook-matrix.md`, `.planning/spikes/jdk25-toolchain.md`, `.planning/spikes/q010-harness/` (scenario runner `run-scenarios.mjs`, sandbox fixture `settings.json`, fixture hooks `deny-all.mjs`, `crash.mjs`, `warn.mjs`)
- New recorded values: pinned Claude Code version string (consumed by plan 01-04 into `.cowork/tiers.json:claudeCode.testedVersion`); per-OS hook-behavior verdicts A1–A4, A9
- New decisions: D-16 posture confirmation (L1 best-effort vs CI-only floor) based on observed stability

<tasks>

<task type="auto">
  <name>Task 1: Q-010 spike — hook behavior matrix on the pinned Claude Code version</name>
  <files>.planning/spikes/q010-hook-matrix.md, .planning/spikes/q010-harness/run-scenarios.mjs, .planning/spikes/q010-harness/fixtures/settings.json, .planning/spikes/q010-harness/fixtures/deny-all.mjs, .planning/spikes/q010-harness/fixtures/crash.mjs, .planning/spikes/q010-harness/fixtures/warn.mjs</files>
  <read_first>
    - .planning/phases/01-dogfood-bootstrap-enforcement/01-RESEARCH.md (Pattern 1 hook mechanics + verified stdin/stdout contracts; Pitfalls 1-2; Open Question 1 scenario list; Assumptions A1-A4, A9)
    - .planning/phases/01-dogfood-bootstrap-enforcement/01-PATTERNS.md (fail-closed deny snippet, settings.json structure — copy-from sources)
    - .planning/phases/01-dogfood-bootstrap-enforcement/01-CONTEXT.md (D-08, D-11, D-16)
  </read_first>
  <action>
    Build a scenario harness under `.planning/spikes/q010-harness/`: a sandbox temp project dir containing a minimal `.claude/settings.json` wiring three fixture hooks — `deny-all.mjs` (always emits `hookSpecificOutput.permissionDecision: "deny"` JSON, exit 0), `crash.mjs` (throws immediately, exits 1 — the deliberate fail-open probe), `warn.mjs` (SessionStart, prints a marker line to stdout). The runner `run-scenarios.mjs` drives headless sessions via `claude -p "<instruction>" --output-format json` inside the sandbox and inspects whether the tool action was blocked.

    Execute the D-20/Open-Question-1 scenario list and record observed behavior per scenario: (1) Write deny, (2) Edit deny, (3) Bash T4 deny, (4) Bash write-bypass attempt (`echo x > t3file`), (5) hook crash fail-mode — does exit 1 block or proceed? (resolves A1), (6) `--dangerously-skip-permissions` — does deny hold? (resolves A3), (7) settings.local.json attempting to remove/override committed hooks (resolves A4), (8) `$CLAUDE_PROJECT_DIR` expansion in the hook command on Windows (resolves A2), (9) SessionStart stdout visibility (resolves A9).

    Record `claude --version` as the pin candidate. OS coverage: Windows executed live on this machine; Linux/macOS marked UNTESTED-LOCALLY with a note that the matrix re-runs on those OSes at first opportunity (D-16 caps the risk: CI L2 is the floor). Write `.planning/spikes/q010-hook-matrix.md` with: version pin, scenario × verdict table, A1-A4/A9 resolutions, PASS/FAIL verdict against D-08 ("hooks go live"), and — if unstable — the D-16 downgrade statement (L1 best-effort + warn, documented limitation).
  </action>
  <verify>
    <automated>node -e "const r=require('fs').readFileSync('.planning/spikes/q010-hook-matrix.md','utf8');for(const k of ['permissionDecision','A1','A2','A3','A4','A9','claude --version'.split(' ')[0]])if(!r.includes(k))throw 'missing: '+k;if(!/PASS|FAIL/.test(r))throw 'no verdict';console.log('OK')"</automated>
    <human-check>End-of-phase: developer opens a live interactive session and confirms the deny message UX (explaining message + next step) matches what the matrix recorded — VALIDATION.md manual-only row for AGENT-03.</human-check>
  </verify>
  <acceptance_criteria>
    - Report contains a 9-row scenario table, each row with an OBSERVED column (not expected-only)
    - A1, A2, A3, A4, A9 each carry an explicit verdict line; untested OS legs explicitly labeled UNTESTED
    - Pinned version string recorded in a machine-findable line `testedVersion: <semver>`
    - Overall verdict PASS (hooks go live, D-08) or FAIL (D-16 downgrade documented) present
  </acceptance_criteria>
  <done>D-08's go-live condition has an empirical basis; plan 01-04 reads `testedVersion` and the fail-mode verdicts from this report.</done>
</task>

<task type="auto">
  <name>Task 2: JDK 25 toolchain smoke on the 3-OS GitHub matrix</name>
  <files>.planning/spikes/jdk25-toolchain.md</files>
  <read_first>
    - .planning/phases/01-dogfood-bootstrap-enforcement/01-RESEARCH.md (Open Question 4; Environment Availability — local JDK is 26, must NOT leak into spike; Standard Stack JDK row)
    - CLAUDE.md (JDK 25 LTS governing decision; ArchUnit ≥1.4.2 pin rationale; MapStruct 1.6.3 / JaCoCo 0.8.15 MEDIUM-confidence rows)
  </read_first>
  <action>
    Create a throwaway repo `<login>/jdk25-toolchain-spike` (gh, private) containing a minimal Maven project: `maven-compiler-plugin` with release 25, dependencies JUnit Jupiter (current 5.x/6.x line), Mockito 5.20.x, MapStruct 1.6.3 (annotation processor configured, one mapper interface compiled), JaCoCo 0.8.15 (report goal bound), ArchUnit 1.4.2 (one trivial rule test). One workflow: matrix `[windows-latest, ubuntu-24.04, macos-latest]`, `actions/setup-java` with `distribution: temurin`, `java-version: 25`, step `./mvnw -B verify` (ship Maven Wrapper 3.9.16 in the spike project). Push, wait for the run via `gh run watch`, capture per-OS results.

    Critical isolation: do NOT run the spike with the local JDK 26 — results must come from the CI matrix's Temurin 25 (RESEARCH Environment Availability). Locally only author files.

    Write `.planning/spikes/jdk25-toolchain.md`: per-OS result table, per-tool verdicts (MapStruct annotation processing on 25, JaCoCo instrumentation on 25, ArchUnit class-file reading, Mockito/Byte Buddy), Temurin 25 availability via setup-java (resolves A7), and the standing guidance line for Phase 2 (pins to carry: ArchUnit ≥1.4.2, JaCoCo ≥0.8.15). Delete the throwaway repo afterwards.
  </action>
  <verify>
    <automated>node -e "const r=require('fs').readFileSync('.planning/spikes/jdk25-toolchain.md','utf8');for(const k of ['temurin','25','MapStruct','JaCoCo','ArchUnit','windows-latest','ubuntu-24.04','macos-latest'])if(!r.includes(k))throw 'missing: '+k;console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - Report shows a 3-OS result table with green/red status per OS from an actual GitHub Actions run (run URL recorded)
    - MapStruct and JaCoCo each have an explicit works-on-25 verdict (the two MEDIUM-confidence items)
    - A7 (Temurin 25 via setup-java) resolved
    - Report states results came from CI Temurin 25, not local JDK 26
  </acceptance_criteria>
  <done>Roadmap success criterion 5's JDK-25 smoke is satisfied; Phase 2 backend toolchain pins are empirically grounded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Headless claude sessions → sandbox filesystem | Scenario runs execute agent actions; must be confined to a temp sandbox dir |
| Spike CI → GitHub Actions | Throwaway repo workflow runs third-party actions |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-07 | Elevation | Hook crash = silent fail-open | mitigate | Scenario 5 deliberately probes crash fail-mode; verdict feeds the fail-closed catch-block design in plan 01-04 |
| T-01-08 | Elevation | `--dangerously-skip-permissions` bypassing deny | mitigate | Scenario 6 tests the product-doc claim empirically (A3); D-16 documents the floor if it fails |
| T-01-09 | Tampering (supply chain) | Spike workflow third-party actions | mitigate | Only `actions/*` official actions (checkout, setup-java), pinned by major version |
| T-01-SC | Tampering | package installs | accept | Spike Maven deps are build-time, isolated in a throwaway repo deleted after; main tree installs nothing |
</threat_model>

<verification>
- Both reports exist and pass the key-presence assertions
- Q-010 report has 9 observed-behavior rows + version pin; JDK report cites a real Actions run URL with 3-OS results
</verification>

<success_criteria>
- D-08 go-live decision for hooks is evidence-backed (PASS) or D-16 downgrade is documented (FAIL)
- A1–A4, A7, A9 resolved
- Roadmap success criterion 5 satisfied for Q-010 + JDK-25 smoke
</success_criteria>

<output>
Create `.planning/phases/01-dogfood-bootstrap-enforcement/01-03-SUMMARY.md` when done. Record the pinned Claude Code version prominently — plan 01-04 writes it into .cowork/tiers.json.
</output>
