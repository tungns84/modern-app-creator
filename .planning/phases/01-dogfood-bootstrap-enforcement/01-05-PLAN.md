---
phase: 01-dogfood-bootstrap-enforcement
plan: 05
type: execute
wave: 2
depends_on: ["01-01", "01-12"]
files_modified:
  - infra/compose.yaml
  - Taskfile.yml
  - .github/workflows/os-matrix.yml
  - .planning/spikes/local-stack-verification.md
autonomous: true
requirements: [FOUND-02]
must_haves:
  truths:
    - "`task up` boots all 6 services (PostgreSQL 16, Valkey 8, Mailpit, MinIO, Keycloak 26.6, grafana/otel-lgtm) and blocks until every healthcheck passes — no hand-rolled wait loops"
    - "`task down` tears the stack down; `task --list` shows descriptions"
    - "CI ubuntu leg runs real `task up` and asserts all services healthy; Windows/macOS legs run container-free smoke (task binary, task --list, compose config validation, JDK 25 smoke) per D-04"
    - "Local Windows `task up` verification is recorded with runtime named; the one-time macOS verification is completed (or explicitly waived in .cowork/waivers.json) BEFORE /gsd-verify-work runs (D-04 manual evidence)"
  artifacts:
    - path: "infra/compose.yaml"
      provides: "6-service local stack with per-service healthchecks"
      contains: "valkey/valkey:8"
      min_lines: 80
    - path: "Taskfile.yml"
      provides: "up/down/ps/logs tasks in POSIX-sh syntax (mvdan/sh, Windows-native)"
      contains: "--wait"
    - path: ".github/workflows/os-matrix.yml"
      provides: "3-OS matrix encoding the D-04 split"
      contains: "macos-latest"
  key_links:
    - from: "Taskfile.yml"
      to: "infra/compose.yaml"
      via: "docker compose -f infra/compose.yaml up -d --wait"
      pattern: "up -d --wait"
    - from: ".github/workflows/os-matrix.yml"
      to: "Taskfile.yml"
      via: "ubuntu leg runs task up; win/mac legs run task --list"
      pattern: "task "
---

<objective>
Deliver FOUND-02: the one-command local stack (`task up`) on Taskfile v3.51.1 + Docker Compose `--wait`, with the 6 pinned images (D-17 Valkey, D-18 otel-lgtm), and the 3-OS CI matrix encoding the D-04 split. Spec unit: specs/003-local-stack (approved in 01-12).

Purpose: Every later phase's dev loop and integration tests stand on this stack; readiness lives in compose healthchecks, not Taskfile loops (Don't-Hand-Roll).
Output: Working local stack + standing os-matrix workflow.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-dogfood-bootstrap-enforcement/01-PATTERNS.md
@.planning/phases/01-dogfood-bootstrap-enforcement/01-RESEARCH.md
@specs/003-local-stack/plan.md
</context>

## Artifacts this phase produces (this plan)

- New file paths: `infra/compose.yaml`, `Taskfile.yml`, `.github/workflows/os-matrix.yml`, `.planning/spikes/local-stack-verification.md`
- New task names (Taskfile): `up`, `down`, `ps`, `logs`
- New compose service names: `postgres`, `valkey`, `mailpit`, `minio`, `keycloak`, `lgtm`
- New CI job names: `stack-ubuntu`, `smoke-windows`, `smoke-macos` (workflow `os-matrix.yml`)

<tasks>

<task type="auto">
  <name>Task 1: infra/compose.yaml — 6 services, healthchecks inside containers, pinned tags</name>
  <files>infra/compose.yaml</files>
  <read_first>
    - .planning/phases/01-dogfood-bootstrap-enforcement/01-PATTERNS.md (Keycloak healthcheck verbatim block; images table with healthcheck column)
    - .planning/phases/01-dogfood-bootstrap-enforcement/01-RESEARCH.md (Standard Stack local images table; Pitfall 7 — healthchecks run INSIDE containers)
  </read_first>
  <action>
    Author `infra/compose.yaml` with exactly these services, images, host ports, and healthchecks:
    - `postgres`: `postgres:16-alpine` (resolve and pin the current 16.x minor tag), port 5432, env POSTGRES_USER/POSTGRES_PASSWORD/POSTGRES_DB = app/app/app (dev-only, comment-labeled), healthcheck `pg_isready -U app`
    - `valkey`: `valkey/valkey:8` (pin current 8.x minor) — D-17, port 6379, healthcheck `valkey-cli ping`
    - `mailpit`: `axllent/mailpit` (resolve current stable tag and pin), ports 1025 (SMTP) + 8025 (UI), healthcheck per assumption A8 — verify the `/livez` endpoint at adoption; if absent, use the image's documented healthcheck
    - `minio`: `minio/minio` (pin current stable), ports 9000 (S3) + 9001 (console), command `server /data --console-address ":9001"`, healthcheck `curl -f http://localhost:9000/minio/health/live` (verify A8 at adoption — minio image ships curl)
    - `keycloak`: `quay.io/keycloak/keycloak:26.6.3`, port 8080, command `start-dev`, env KC_HEALTH_ENABLED=true + KC_BOOTSTRAP_ADMIN_USERNAME/PASSWORD=admin/admin (dev-only labeled), healthcheck = the /dev/tcp CMD-SHELL probe against mgmt port 9000 copied verbatim from PATTERNS.md (image has no curl); do NOT publish mgmt port 9000 to the host (MinIO owns host 9000). Realm wiring is Phase 3 scope (deferred — container only)
    - `lgtm`: `grafana/otel-lgtm` (resolve + pin current stable tag) — D-18, ports 3000 (Grafana) / 4317 (OTLP gRPC) / 4318 (OTLP HTTP), healthcheck HTTP on 3000
    All healthchecks run inside containers (Pitfall 7). Set `start_period`/`interval`/`retries` per PATTERNS (Keycloak: 30s/10s/12). Validate with `docker compose -f infra/compose.yaml config -q`.
  </action>
  <verify>
    <automated>docker compose -f infra/compose.yaml config -q && node -e "const y=require('fs').readFileSync('infra/compose.yaml','utf8');for(const k of ['postgres:16','valkey/valkey:8','axllent/mailpit','minio/minio','keycloak:26.6.3','grafana/otel-lgtm'])if(!y.includes(k))throw 'missing: '+k;if((y.match(/healthcheck/g)||[]).length<6)throw 'not all services have healthchecks';console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `docker compose config -q` exits 0
    - All 6 images present with pinned tags (no bare `latest` anywhere in the file)
    - 6 healthcheck blocks; Keycloak healthcheck targets container port 9000 via /dev/tcp; host port 9000 maps to MinIO only
    - Dev-only credentials carry an inline comment marking them non-production
  </acceptance_criteria>
  <done>Compose definition is valid, fully pinned, healthcheck-complete (FOUND-02 substrate).</done>
</task>

<task type="auto">
  <name>Task 2: Taskfile.yml + live local `task up` on Windows (D-04 manual evidence)</name>
  <files>Taskfile.yml, .planning/spikes/local-stack-verification.md</files>
  <read_first>
    - .planning/phases/01-dogfood-bootstrap-enforcement/01-PATTERNS.md (Taskfile verbatim block; mvdan/sh rule — only docker/node/task callable)
    - .planning/phases/01-dogfood-bootstrap-enforcement/01-RESEARCH.md (Environment Availability — go-task NOT installed locally; `winget install Task.Task`)
  </read_first>
  <action>
    Install go-task locally if absent (`winget install Task.Task` — pinned line v3.51.1, record the installed version). Author `Taskfile.yml` (version '3') with tasks: `up` (desc "Boot the full local stack (waits for healthchecks)", cmd `docker compose -f infra/compose.yaml up -d --wait`), `down` (`docker compose -f infra/compose.yaml down`), `ps` (`docker compose -f infra/compose.yaml ps`), `logs` (`docker compose -f infra/compose.yaml logs -f --tail=100`). POSIX-sh syntax only; call only `docker`/`node`/`task` executables (Pattern 4). Do NOT add a `verify` task here — plan 01-11 owns it (Taskfile overlap is sequenced via depends_on).

    Run `task up` on this Windows machine, wait for `--wait` to return, capture `docker compose -f infra/compose.yaml ps --format json` and assert all 6 services healthy. Write `.planning/spikes/local-stack-verification.md`: OS + Docker runtime name/version (D-04 requires naming the runtime — e.g., Docker Desktop with WSL2 backend; Pitfall 7 clarification: "no WSL" means devs do not work inside WSL — daemon backend is acceptable), per-service health result, `task --version` output. Add the macOS row marked PENDING with this binding note (D-04 requires the verification DURING Phase 1, not open-ended): "BLOCKING for phase verification — must be either completed and recorded here, or explicitly waived via a time-boxed entry in .cowork/waivers.json (scope: macos-task-up-verification), BEFORE /gsd-verify-work runs." Run `task down` after.
  </action>
  <verify>
    <automated>task --list && node -e "const r=require('fs').readFileSync('.planning/spikes/local-stack-verification.md','utf8');for(const k of ['healthy','Windows','BLOCKING for phase verification'])if(!r.includes(k))throw 'missing: '+k;console.log('OK')"</automated>
    <human-check>BEFORE /gsd-verify-work (not open-ended): developer confirms the Windows `task up` evidence AND resolves the macOS row — either run the one-time macOS `task up` verification and record the results, or add an explicit time-boxed waiver entry (scope: macos-task-up-verification) to .cowork/waivers.json (D-04). Phase verification must not start while the macOS row is an unresolved PENDING.</human-check>
  </verify>
  <acceptance_criteria>
    - `task --list` shows up/down/ps/logs with descriptions
    - Verification report shows all 6 services reached healthy on Windows with the Docker runtime named
    - macOS row carries the BLOCKING note tying its resolution (verification or waiver) to before /gsd-verify-work
    - Taskfile contains the literal `up -d --wait` (readiness delegated to compose, zero wait loops)
  </acceptance_criteria>
  <done>`task up` works end-to-end on Windows without WSL-as-workspace; D-04 manual-evidence half recorded with a bounded macOS deadline.</done>
</task>

<task type="auto">
  <name>Task 3: os-matrix.yml — D-04 split on [windows-latest, ubuntu-24.04, macos-latest]</name>
  <files>.github/workflows/os-matrix.yml</files>
  <read_first>
    - .planning/phases/01-dogfood-bootstrap-enforcement/01-PATTERNS.md (os-matrix section — exact leg contents)
    - .planning/spikes/jdk25-toolchain.md (setup-java parameters proven by the spike)
  </read_first>
  <action>
    Author `.github/workflows/os-matrix.yml`, trigger `on: [push, pull_request]`, `permissions: contents: read`. Jobs:
    - `stack-ubuntu` (ubuntu-24.04): install go-task pinned v3.51.1 (official sh installer with explicit version arg), `task up`, assert healthy via `docker compose -f infra/compose.yaml ps --format json` parsed by an inline `node -e` (every service Health == "healthy"), then `task down`
    - `smoke-windows` (windows-latest): install task pinned, `task --version`, `task --list`, `docker compose -f infra/compose.yaml config -q`, `actions/setup-java` (distribution temurin, java-version 25) + assert `java -version` output contains "25"
    - `smoke-macos` (macos-latest): same as the windows leg, except compose validation — macOS runners ship no Docker CLI: install the standalone `docker-compose` binary (pinned release download or `brew install docker-compose`) and run `docker-compose -f infra/compose.yaml config -q`
    Only `actions/*` official actions, pinned by major (Security V14). Workflow comment: the JDK leg is a minimal standing smoke; the full toolchain matrix ran once in the jdk25 spike; Phase 2's real `./mvnw verify` supersedes it. Push and confirm green via `gh run watch`.
  </action>
  <verify>
    <automated>node -e "const y=require('fs').readFileSync('.github/workflows/os-matrix.yml','utf8');for(const k of ['ubuntu-24.04','windows-latest','macos-latest','task up','temurin','config -q'])if(!y.includes(k))throw 'missing: '+k;console.log('OK')" && gh run list --workflow=os-matrix.yml --limit 1</automated>
  </verify>
  <acceptance_criteria>
    - Workflow contains 3 jobs matching the D-04 split exactly (real `task up` only on ubuntu)
    - Latest os-matrix run on GitHub is green (gh run list shows completed/success)
    - go-task installer pinned to v3.51.1 on all legs; setup-java pinned distribution temurin / 25
  </acceptance_criteria>
  <done>Success criterion 3's CI half holds: ubuntu proves the stack, win/mac prove the toolchain portability (D-04).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Compose images ← public registries | Supply-chain surface of Phase 1 is Docker images, not npm |
| Dev-only credentials in compose | Committed literals must be unambiguous as non-production |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-14 | Tampering (supply chain) | 6 Docker images | mitigate | Official/org images only, pinned tags (no `latest`); digest-pinning optional follow-up; secret/CVE scan gate arrives Phase 6 (GATE-08) — gap noted in compose comments |
| T-01-15 | Information disclosure | admin/admin + app/app dev creds committed | accept | Dev-only, comment-labeled; no real secrets exist in Phase 1 (RESEARCH Security Domain); explicit ONBOARDING note deferred to Phase 8 |
| T-01-16 | Denial of service | Keycloak healthcheck flakiness blocking `--wait` | mitigate | start_period 30s + 12 retries per PATTERNS; healthcheck runs in-container (no host curl quirks) |
| T-01-SC | Tampering | package installs | accept | No npm/pip installs; go-task installed via OS package manager pinned v3.51.1 |
</threat_model>

<verification>
- `docker compose config -q` green; local `task up` reached all-healthy on Windows (report on file)
- os-matrix workflow green on GitHub for all 3 legs
- macOS row resolved (verified or waived) before /gsd-verify-work (D-04)
</verification>

<success_criteria>
- One command boots the full stack with healthcheck-gated readiness (FOUND-02)
- D-04 split encoded in CI; manual Windows evidence recorded; macOS one-time verification completed or explicitly waived (time-boxed register entry) BEFORE phase verification — never an open-ended PENDING
- Valkey (D-17) and otel-lgtm (D-18) are the shipped choices
</success_criteria>

<output>
Create `.planning/phases/01-dogfood-bootstrap-enforcement/01-05-SUMMARY.md` when done.
</output>
