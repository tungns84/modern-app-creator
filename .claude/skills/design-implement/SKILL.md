---
name: design-implement
description: Implement a Stitch design as shadcn/ui + tokens code — use when asked to turn a design screen into frontend components (design workflow currently OFF).
---

# design-implement — Skeleton contract (implemented in Phase 4)

## CONTRACT

tier: T2 — feature-level implementation following an approved design.

**Inputs:** Stitch screen reference (via Stitch MCP `get_screen_code` /
`get_screen_image`), target feature folder.

**Outputs:**
- shadcn/ui-based implementation using design tokens only (no raw hex/px)
- DESIGN-WORKFLOW.md checklist completed

## Status

Skeleton — implemented in Phase 4; the design workflow is currently
OFF/deferred (no `.mcp.json` Stitch entry is emitted yet). Do NOT improvise
an implementation from this contract; report to the human instead.
