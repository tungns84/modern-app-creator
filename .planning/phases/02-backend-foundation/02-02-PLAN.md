---
phase: 02-backend-foundation
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - .claude/skills/new-module/SKILL.md
  - scripts/new-module/scaffold.mjs
  - scripts/new-module/tests/scaffold.test.mjs
  - backend/src/main/java/com/acme/app/appconfig/package-info.java
  - backend/src/main/java/com/acme/app/appconfig/EventRetryProperties.java
  - backend/src/main/java/com/acme/app/appconfig/EventRetentionProperties.java
  - backend/src/main/java/com/acme/app/appconfig/ObservabilityProperties.java
  - backend/src/main/java/com/acme/app/appconfig/AppConfigPropertiesRegistration.java
  - backend/src/main/java/com/acme/app/appconfig/spi/package-info.java
  - backend/src/main/resources/application-consul.yml
  - backend/src/test/java/com/acme/app/appconfig/AppConfigModuleTest.java
  - backend/src/test/java/com/acme/app/appconfig/AppConfigPropertiesValidationTest.java
  - backend/src/test/java/com/acme/app/ModulithVerifyTest.java
  - specs/009-appconfig-module/spec.md
  - specs/009-appconfig-module/plan.md
autonomous: false
requirements: [FOUND-10, AGENT-08]

must_haves:
  truths:
    - "The `new-module` skill produces a DAG-correct module scaffold (package-info with allowedDependencies, optional spi/ + events/, Flyway folder when tables exist, @ApplicationModuleTest stub, name-set bump, pre-filled specs plan.md)"
    - "`appconfig` was generated WITH the skill (first of 4 dogfood runs) and the skill validates against its CONTRACT"
    - "Every config value in the app is read via @ConfigurationProperties records — no @Value or raw Environment outside appconfig (enforced by GATE-02 D-09 rules)"
    - "Typed properties reject invalid values at startup (@Validated fast-fail)"
    - "Config source switches by Spring profile: env+ConfigMap (default) or Consul KV (consul profile) on one classpath"
  artifacts:
    - path: ".claude/skills/new-module/SKILL.md"
      provides: "full implementation replacing the Phase 1 skeleton contract (D-01)"
      contains: "CONTRACT"
    - path: "backend/src/main/java/com/acme/app/appconfig/package-info.java"
      provides: "appconfig module boundary allowedDependencies = {shared}, exposes ::spi"
      contains: "@ApplicationModule"
    - path: "backend/src/main/java/com/acme/app/appconfig/EventRetryProperties.java"
      provides: "bounded-retry typed properties (maxAttempts, initialDelay, multiplier, maxDelay) with @Validated defaults"
      contains: "@ConfigurationProperties"
    - path: "backend/src/main/java/com/acme/app/appconfig/EventRetentionProperties.java"
      provides: "EPR completed-record retention typed property (D-10)"
      contains: "@ConfigurationProperties"
    - path: "backend/src/main/resources/application-consul.yml"
      provides: "consul-profile config source switch (D-07) — gated on Q-1 verdict"
  key_links:
    - from: "backend/src/main/java/com/acme/app/appconfig/spi/package-info.java"
      to: "backend/src/main/java/com/acme/app/appconfig/EventRetryProperties.java"
      via: "::spi named interface exposing the typed-property records to other modules"
      pattern: "NamedInterface|spi"
    - from: "backend/src/test/java/com/acme/app/ModulithVerifyTest.java"
      to: "backend/src/main/java/com/acme/app/appconfig/package-info.java"
      via: "name-set now expects {shared, appconfig}"
      pattern: "appconfig"
---

<objective>
Codify the `new-module` skill from the `shared` reference shape built in Plan 01 (D-01: full implementation replacing the Phase 1 skeleton contract), then DOGFOOD it by generating the `appconfig` module — the typed-properties infrastructure that every other module reads config through. `appconfig` ships the `@ConfigurationProperties` records (event retry, retention, observability) exposed via `appconfig::spi`, the profile-based config-source switch (env+ConfigMap default / Consul KV under the `consul` profile, D-07), and is the ONLY module where `@Value`/`Environment` are permitted (D-09).

Purpose: `appconfig::spi` is the dependency root for `i18n`, `caching`, and `observability` (Plans 03/04) and supplies the typed properties the EPR retry/retention logic (Plan 05) consumes. This is the first real exercise of the `new-module` skill under application code.
Output: Implemented `new-module` skill + scaffold tooling; `appconfig` module with typed properties and the Consul profile switch (FOUND-10).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/02-backend-foundation/02-CONTEXT.md
@.planning/phases/02-backend-foundation/02-RESEARCH.md
@.planning/phases/02-backend-foundation/02-PATTERNS.md
@backend/CLAUDE.md
@.claude/skills/new-module/SKILL.md
@.claude/skills/plan/SKILL.md
@.cowork/tiers.json
@.planning/spikes/q1-spring-cloud-consul-compat.md
@.planning/phases/02-backend-foundation/02-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement the new-module skill (codify the shared reference shape)</name>
  <files>.claude/skills/new-module/SKILL.md, scripts/new-module/scaffold.mjs, scripts/new-module/tests/scaffold.test.mjs</files>
  <read_first>
    - .claude/skills/new-module/SKILL.md (the CONTRACT to implement — inputs/outputs locked)
    - .planning/phases/02-backend-foundation/02-01-SUMMARY.md (the actual shared module shape produced by Plan 01)
    - backend/src/main/java/com/acme/app/shared/package-info.java (reference boundary declaration)
    - .planning/phases/02-backend-foundation/02-RESEARCH.md (Pattern 1 module boundary; Pattern 3 @ApplicationModuleTest; Pattern 7 per-module Flyway + Q-4 explicit FlywayConfigurationCustomizer)
    - .claude/skills/plan/SKILL.md (pre-filled plan.md template the skill must emit)
    - scripts/checks/run-gate.mjs (existing scaffold/test conventions — node:test, file-list globbing)
  </read_first>
  <behavior>
    - Given inputs (module name, allowedDependencies list, exposesSpi flag, exposesEvents flag, hasTables flag), the scaffold produces: `package-info.java` with `@ApplicationModule(allowedDependencies = {...})`; `spi/package-info.java` declaring a `@NamedInterface("spi")` when exposesSpi; `events/` package when exposesEvents; `db/migration/<module>/` folder only when hasTables; an `@ApplicationModuleTest` stub; and a pre-filled `specs/NNN-<module>/plan.md` with `Approved-by:` EMPTY.
    - The scaffold bumps the `ModulithVerifyTest` BASE_MODULES name set to include the new module.
    - Generating a module twice (idempotency) does not corrupt existing files.
  </behavior>
  <action>
    Replace `.claude/skills/new-module/SKILL.md` Status section: change from "Skeleton — implemented in Phase 2" to the full implementation, keeping the CONTRACT section's inputs/outputs intact (do NOT change outputs without T3 review). Implement the generator as `scripts/new-module/scaffold.mjs` (node, zero npm deps, cross-OS — only `node` allowed per Taskfile executable rule). The script takes module metadata and writes the outputs listed in the CONTRACT, deriving `allowedDependencies` rendering (`"shared"`, `"appconfig::spi"`, etc.), the `@NamedInterface("spi")` package-info, the Flyway folder only when `hasTables`, the `@ApplicationModuleTest` stub, and the pre-filled `specs/NNN-<module>/plan.md` (next NNN computed by scanning `specs/[0-9][0-9][0-9]-*`, never `000`). It MUST also append the new module name to `ModulithVerifyTest.BASE_MODULES` and register the module's Flyway location in the `FlywayConfigurationCustomizer` (Q-4: explicit registration, the safer default) when `hasTables`. Write `scripts/new-module/tests/scaffold.test.mjs` (node:test) asserting the CONTRACT outputs for a sample module and idempotency. The SKILL.md drives the agent to run this scaffold rather than improvising layout. `.claude/skills/**` and `scripts/checks/**` are constitution/T3-adjacent — author the bound spec in Task 2 covering both the skill and the appconfig module.
  </action>
  <verify>
    <automated>node --test scripts/new-module/tests/scaffold.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `node --test scripts/new-module/tests/scaffold.test.mjs` passes, asserting every CONTRACT output (package-info, spi/events when declared, Flyway folder only when hasTables, @ApplicationModuleTest stub, pre-filled plan.md with empty Approved-by) and idempotency.
    - `.claude/skills/new-module/SKILL.md` Status no longer says "Skeleton"; CONTRACT inputs/outputs unchanged.
    - The scaffold bumps `ModulithVerifyTest.BASE_MODULES` and registers Flyway locations explicitly (Q-4).
  </acceptance_criteria>
  <done>The `new-module` skill is a working generator validated by tests; ready for 4 dogfood runs (appconfig here, i18n/caching/observability in Plans 03/04).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Generate appconfig WITH the skill — typed properties + Consul profile switch</name>
  <files>backend/src/main/java/com/acme/app/appconfig/package-info.java, backend/src/main/java/com/acme/app/appconfig/EventRetryProperties.java, backend/src/main/java/com/acme/app/appconfig/EventRetentionProperties.java, backend/src/main/java/com/acme/app/appconfig/ObservabilityProperties.java, backend/src/main/java/com/acme/app/appconfig/AppConfigPropertiesRegistration.java, backend/src/main/java/com/acme/app/appconfig/spi/package-info.java, backend/src/main/resources/application-consul.yml, backend/pom.xml, backend/src/test/java/com/acme/app/appconfig/AppConfigModuleTest.java, backend/src/test/java/com/acme/app/appconfig/AppConfigPropertiesValidationTest.java, backend/src/test/java/com/acme/app/ModulithVerifyTest.java, specs/009-appconfig-module/spec.md, specs/009-appconfig-module/plan.md</files>
  <read_first>
    - .planning/phases/02-backend-foundation/02-PATTERNS.md (EventRetryProperties typed-properties record pattern; application-consul.yml; @ApplicationModuleTest pattern)
    - .planning/phases/02-backend-foundation/02-RESEARCH.md (Config Properties + Consul Switch section; Pattern 5 bounded-retry properties; D-09/D-10/D-12 property keys; Open Question Q-1)
    - .planning/phases/02-backend-foundation/02-CONTEXT.md (D-07 Consul dependency policy, D-08 Testcontainers-only proof, D-09 @Value ban, D-10 retention, D-12 retry/staleness)
    - .planning/spikes/q1-spring-cloud-consul-compat.md (Q-1 VERDICT — COMPATIBLE adds the BOM + consul profile; BLOCKED defers Consul)
    - backend/pom.xml (where the spring-cloud-dependencies BOM + spring-cloud-consul-config would be added)
    - .claude/skills/plan/SKILL.md (bound-spec format)
  </read_first>
  <behavior>
    - `@Validated` `@ConfigurationProperties` records reject out-of-range values at startup (e.g. maxAttempts=0 fails fast; verified via a binding test).
    - Defaults bind when no overrides are present (maxAttempts=3, initialDelay=PT10S, multiplier=2.0, maxDelay=PT5M per D-12; retention per D-10).
    - Other modules can inject the records via `appconfig::spi`; appconfig is the only module allowed `@Value`/`Environment` (GATE-02 D-09 rules still pass).
    - Under the `consul` profile, the same typed properties resolve from Consul KV instead of env — IF Q-1 verdict is COMPATIBLE.
  </behavior>
  <action>
    Author the bound spec `specs/009-appconfig-module/{spec.md,plan.md}` (tier T3 — package-info.java + the new-module skill change + pom.xml dependency add are T3; `Approved-by:` EMPTY); bind branch `feat/009-appconfig-module`. Run the `new-module` scaffold for `appconfig` with allowedDependencies={shared}, exposesSpi=true, exposesEvents=false, hasTables=false (no appconfig tables in Phase 2 — D-04). Then fill the generated module: `EventRetryProperties` (`@ConfigurationProperties("acme.events.retry")` `@Validated` record with `@Min(1)@Max(10) int maxAttempts`, `@NotNull Duration initialDelay`, `double multiplier`, `@NotNull Duration maxDelay`, canonical no-arg constructor giving the D-12 defaults), `EventRetentionProperties` (`@ConfigurationProperties("acme.events.retention")` for the D-10 completed-record retention Duration), and `ObservabilityProperties` (`@ConfigurationProperties("acme.observability")` holding the MDC PII allowlist field list + staleness thresholds — consumed by observability module in Plan 04). Register all records via `@EnableConfigurationProperties` in `AppConfigPropertiesRegistration` (constructor injection only). Declare `appconfig/spi/package-info.java` with `@NamedInterface("spi")` and ensure the property records are reachable through it. Wire the config-source switch: default `application.yml` uses env+ConfigMap (`spring.config.import: optional:file:.env[.properties]`); `application-consul.yml` adds the Consul source. Q-1 GATE: if the Plan-01 spike verdict is COMPATIBLE, add the named `spring-cloud-dependencies` BOM + `spring-cloud-consul-config` to backend/pom.xml and ship `application-consul.yml` per PATTERNS; if BLOCKED, ship only the env+ConfigMap source, add a clearly-commented placeholder `application-consul.yml`, and record the Consul deferral in the SUMMARY (the Plan 05 Consul Testcontainers test is then skipped). Write `AppConfigModuleTest` (`@ApplicationModuleTest`) asserting defaults bind and are valid, and `AppConfigPropertiesValidationTest` (unit) asserting an invalid value (maxAttempts=0) fails binding. Bump `ModulithVerifyTest.BASE_MODULES` to `{shared, appconfig}` (the scaffold does this; confirm).
  </action>
  <verify>
    <automated>./mvnw -q test -Dtest=AppConfigModuleTest,AppConfigPropertiesValidationTest,ModulithVerifyTest,ArchitectureGatesTest -f backend/pom.xml</automated>
  </verify>
  <acceptance_criteria>
    - `AppConfigModuleTest` confirms the `@Validated` records bind valid defaults; `AppConfigPropertiesValidationTest` confirms an invalid value fails binding at startup.
    - `ModulithVerifyTest` now asserts the name set `{shared, appconfig}` (+`bpm` when enabled) and `modules.verify()` passes.
    - `ArchitectureGatesTest` still passes: `@Value`/`Environment` appear (if at all) only inside `com.acme.app.appconfig`.
    - `appconfig/spi/package-info.java` declares `@NamedInterface("spi")`; the property records are exposed through it.
    - Consul handling matches the Q-1 verdict: COMPATIBLE → BOM + consul profile shipped; BLOCKED → env source only with deferral recorded.
  </acceptance_criteria>
  <done>`appconfig` exists as the typed-properties root (FOUND-10); config source switches by profile; the `new-module` skill is validated by its first dogfood run.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| config source (env / Consul KV) → app | typed properties cross from an external config store into the running app via profile-selected `spring.config.import` |
| build → main branch | new module boundary (package-info), pom dependency add, and skill change are T3 — bound spec + H2 required |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04 | Information disclosure | Config property values (incl. Consul KV secrets) exposed via Actuator `/actuator/env` | mitigate | Rely on Boot 4 default `management.endpoint.env.show-values=NEVER`; assert the actuator exposure list (set in Plan 04) excludes raw env values; document in SUMMARY |
| T-02-05 | Tampering | Unverified `spring-cloud-consul-config` / BOM pinned without a compatibility verdict | mitigate | Task 2 adds the Consul BOM ONLY when the Plan-01 Q-1 spike says COMPATIBLE with a named, confidence-labelled version; BLOCKED defers it |
| T-02-06 | Spoofing/Tampering | Consul KV source trusted as authoritative config without channel context | accept | Phase 2 proves the seam via Testcontainers only (D-08); production Consul TLS/ACL hardening is deferred (Phase 6 territory per CONTEXT deferred ideas) — low risk in the dev/seam scope |
| T-02-SC | Tampering | npm/Maven installs (new-module scaffold has zero npm deps; pom Consul add) | mitigate | scaffold.mjs uses only the node stdlib (no new npm packages); the only new Maven artifact (Consul) is gated on the Q-1 verdict and reviewed at H2 |
</threat_model>

<verification>
- `./mvnw -q test -Dtest=AppConfigModuleTest,AppConfigPropertiesValidationTest,ModulithVerifyTest,ArchitectureGatesTest -f backend/pom.xml` passes.
- `node --test scripts/new-module/tests/scaffold.test.mjs` passes.
- `node scripts/checks/run-gate.mjs --mode fast` stays green and under budget.
</verification>

<success_criteria>
- The `new-module` skill is implemented (not a skeleton) and produces CONTRACT-correct scaffolds, validated by tests.
- `appconfig` is generated WITH the skill (dogfood run 1/4) and holds the typed-property records exposed via `appconfig::spi`.
- All config is read via `@ConfigurationProperties` records; `@Value`/`Environment` confined to appconfig (D-09 / GATE-02).
- Invalid config fails fast at startup (`@Validated`).
- Config source switches by profile (FOUND-10); Consul path follows the Q-1 verdict.
</success_criteria>

<artifacts_this_phase_produces>
- Skill: `.claude/skills/new-module/SKILL.md` (full implementation); `scripts/new-module/scaffold.mjs`; `scripts/new-module/tests/scaffold.test.mjs`.
- Module: `com.acme.app.appconfig` (`@ApplicationModule(allowedDependencies = {"shared"})`) + `appconfig.spi` (`@NamedInterface("spi")`).
- Typed properties: `EventRetryProperties` (`acme.events.retry`), `EventRetentionProperties` (`acme.events.retention`), `ObservabilityProperties` (`acme.observability`); registration bean `AppConfigPropertiesRegistration`.
- Config: `application-consul.yml` (consul profile source switch — shipped or placeholder per Q-1 verdict); pom Consul BOM addition conditional on Q-1.
- Tests: `AppConfigModuleTest` (`@ApplicationModuleTest`), `AppConfigPropertiesValidationTest` (unit).
- Test update: `ModulithVerifyTest.BASE_MODULES` → `{shared, appconfig}`.
- Spec: `specs/009-appconfig-module/{spec,plan}.md`.
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/02-backend-foundation/02-02-SUMMARY.md` when done.
</output>
