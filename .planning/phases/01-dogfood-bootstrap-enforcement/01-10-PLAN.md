---
phase: 01-dogfood-bootstrap-enforcement
plan: 10
type: execute
wave: 3
depends_on: ["01-05", "01-07"]
files_modified:
  - templates/claude/ROOT-CLAUDE.template.md
  - templates/claude/smoke-commands.json
  - backend/CLAUDE.md
  - frontend/CLAUDE.md
  - .github/workflows/claude-md-check.yml
autonomous: true
requirements: [AGENT-01, GATE-12]
must_haves:
  truths:
    - "The preset root CLAUDE.md exists ONLY as a template source with placeholders — never as a maintained static file in the working tree (D-13); CI renders it with sample values and asserts ≤200 lines (D-14)"
    - "backend/CLAUDE.md and frontend/CLAUDE.md live in place with literal com.acme.app values, each ≤150 lines (D-15)"
    - "Command smoke runs every declared command per OS leg and fails on zero declared commands (GATE-12, Pitfall 9)"
    - "CLAUDE.md size-budget and command-smoke checks block CI (success criterion 2, second half)"
  artifacts:
    - path: "templates/claude/ROOT-CLAUDE.template.md"
      provides: "Source of the preset root CLAUDE.md (D-13/D-14)"
      contains: "{{projectName}}"
    - path: "templates/claude/smoke-commands.json"
      provides: "Explicit smoke-able command declaration (Pitfall 9 — no prose extraction)"
      contains: "task --list"
    - path: "backend/CLAUDE.md"
      provides: "Backend constitution ≤150 lines, literal com.acme.app (D-15)"
      contains: "com.acme.app"
    - path: "frontend/CLAUDE.md"
      provides: "Frontend constitution ≤150 lines (D-15)"
      contains: "shared"
    - path: ".github/workflows/claude-md-check.yml"
      provides: "GATE-12 CI wiring (render-then-check, D-14)"
      contains: "claude-md-check.mjs"
  key_links:
    - from: ".github/workflows/claude-md-check.yml"
      to: "scripts/checks/claude-md-check.mjs"
      via: "CI invokes the plan-01-07 tooling on real content"
      pattern: "claude-md-check"
    - from: "scripts/checks/claude-md-check.mjs"
      to: "templates/claude/ROOT-CLAUDE.template.md"
      via: "render with sample values projectName=smoke groupId=com.acme artifactId=app"
      pattern: "ROOT-CLAUDE"
---

<objective>
Author the three-layer CLAUDE.md content (AGENT-01) under the D-13/D-14/D-15 regime and wire GATE-12 into CI using the plan-01-07 tooling: root = template source rendered-then-checked (≤200 lines); backend/frontend = in-place literals (≤150 lines each); explicit smoke-command manifest. Spec unit: specs/005-claude-pack (approved in 01-01).

Purpose: The PO decision (D-13: "CLAUDE.template.md thuộc về preset nên chỉ sinh ra khi khởi tạo preset") forbids a maintained static root file; size budgets are the product's own research finding (brevity is a feature). The content split table in CLAUDE-CODE-RUNTIME §1 is the authoritative outline.
Output: Template source + two tree constitutions + blocking claude-md-check workflow.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@product/cli/CLAUDE-CODE-RUNTIME.md
@.planning/phases/01-dogfood-bootstrap-enforcement/01-PATTERNS.md
@scripts/checks/claude-md-check.mjs
@scripts/checks/render-claude-md.mjs
@specs/005-claude-pack/plan.md
</context>

## Artifacts this phase produces (this plan)

- New file paths: `templates/claude/ROOT-CLAUDE.template.md`, `templates/claude/smoke-commands.json`, `backend/CLAUDE.md`, `frontend/CLAUDE.md`, `.github/workflows/claude-md-check.yml`
- New placeholders (template): `{{projectName}}`, `{{groupId}}`, `{{artifactId}}` (the PRESET-SPEC §3 placeholder set)
- New CI job name: `claude-md-check`
- New manifest entries (`smoke-commands.json`): `task --list` (all legs), `task up --dry` (all legs — dry-run needs no Docker), `node --version` (all legs); Docker-needing commands restricted to ubuntu leg

<tasks>

<task type="auto">
  <name>Task 1: ROOT-CLAUDE.template.md + smoke-commands.json (D-13/D-14)</name>
  <files>templates/claude/ROOT-CLAUDE.template.md, templates/claude/smoke-commands.json</files>
  <read_first>
    - product/cli/CLAUDE-CODE-RUNTIME.md (§1 content-split table — the authoritative root outline: project ¶, canonical commands, workspace map, compact risk-tier table, pointers, anti-stack "Do not" list; NEVER stack tutorials or tree duplication)
    - product/methodology/AI-COWORK.md (§6 tier table — source for the compact risk-tier table)
    - scripts/checks/render-claude-md.mjs (placeholder syntax the renderer expects)
  </read_first>
  <action>
    Author `templates/claude/ROOT-CLAUDE.template.md` in English with placeholders {{projectName}}/{{groupId}}/{{artifactId}}, containing exactly the §1 root row: one-paragraph project description; canonical commands section (`task up`, `task down`, `task verify`, `./mvnw verify` [arrives Phase 2], `npm run dev/build/codegen` [arrives Phase 4] — mark not-yet-existing commands clearly so smoke doesn't run them); workspace map (backend/, frontend/, infra/, docs/, specs/, scripts/, .claude/, .cowork/); compact T1–T4 risk-tier table (4 rows, from AI-COWORK §6); pointers to docs/methodology/AI-COWORK.md, docs/ARCHITECTURE.md (arrives Phase 2), docs/adr/ index; "Do not" anti-stack list (no GraphQL, no SSR/Next.js, no H2, no Redux, no MUI, no message broker v1). Budget: rendered output ≤200 lines — target ~170 to leave growth room. Never: stack tutorials, duplication of tree-file content.

    Author `templates/claude/smoke-commands.json` in the plan-01-07 manifest format with the entries listed in "Artifacts this phase produces". Only commands that exist NOW (Phase 1) are declared — the manifest grows as phases add commands (GATE-12 extends to ONBOARDING in Phase 8).
  </action>
  <verify>
    <automated>node -e "const t=require('fs').readFileSync('templates/claude/ROOT-CLAUDE.template.md','utf8');for(const k of ['{{projectName}}','task up','T3','Do not'])if(!t.includes(k))throw 'missing: '+k;const m=require('./templates/claude/smoke-commands.json');if(!m.commands||m.commands.length<2)throw 'manifest thin';console.log('OK')" && node scripts/checks/claude-md-check.mjs --root-template templates/claude/ROOT-CLAUDE.template.md --smoke-manifest templates/claude/smoke-commands.json --leg windows</automated>
  </verify>
  <acceptance_criteria>
    - Template renders clean (no orphan {{) with sample values and the rendered output is ≤200 lines (the check CLI exits 0)
    - Risk-tier table present (4 tiers); anti-stack list present; no React/Spring tutorial content
    - Manifest declares ≥2 commands runnable on all legs; smoke executes green locally on Windows
  </acceptance_criteria>
  <done>Preset root CLAUDE.md exists as source-only per the PO decision (D-13); render-then-check passes locally (D-14).</done>
</task>

<task type="auto">
  <name>Task 2: backend/CLAUDE.md + frontend/CLAUDE.md in place (D-15)</name>
  <files>backend/CLAUDE.md, frontend/CLAUDE.md</files>
  <read_first>
    - product/cli/CLAUDE-CODE-RUNTIME.md (§1 content-split rows for backend and frontend — authoritative outlines)
    - .planning/phases/01-dogfood-bootstrap-enforcement/01-RESEARCH.md (State of the Art "Deprecated/outdated" — agent-hallucination warnings to carry forward)
  </read_first>
  <action>
    Author both files in English with literal `com.acme.app` values (D-15 — no placeholders, no live conditional branches; designWorkflow OFF, strictness standard, BPM ON are fixed for Giai đoạn 1; scripts/init renames the literals).

    `backend/CLAUDE.md` (≤150 lines): module DAG summary placeholder pointing to the `new-module` skill; events-for-writes / `::spi`-for-reads rule; test conventions (`*Test`/`*Tests`, Testcontainers on PostgreSQL only); Flyway per-module layout; ArchUnit expectations phrased "you will fail the build if ..." (field injection, bare @Scheduled, native query outside wrapper, entity across controller boundary); agent-hallucination warnings: Testcontainers is 2.x (1.x idioms WILL be wrong), Jackson is 3.x (`tools.jackson` packages — Jackson 2 imports WILL be wrong), JDK is 25 LTS. Never: frontend content, root duplication.

    `frontend/CLAUDE.md` (≤150 lines): zone rules `shared → features → app` (no cross-feature imports); tokens-only styling (no raw hex/px); i18n key workflow (every key in vi AND en or the parity gate fails); a11y requirements (IconButton aria-label, axe gate WCAG 2.2 AA); generated-code rule (never edit `src/generated/`). Never: backend content, root duplication.
  </action>
  <verify>
    <automated>node scripts/checks/claude-md-check.mjs --tree-file backend/CLAUDE.md --tree-file frontend/CLAUDE.md && node -e "const b=require('fs').readFileSync('backend/CLAUDE.md','utf8');for(const k of ['com.acme.app','ArchUnit','Testcontainers'])if(!b.includes(k))throw 'backend missing: '+k;const f=require('fs').readFileSync('frontend/CLAUDE.md','utf8');for(const k of ['shared','features','generated'])if(!f.includes(k))throw 'frontend missing: '+k;console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - Both files pass the ≤150-line budget via the real check CLI (CRLF-normalized count)
    - backend file contains literal com.acme.app, the four ArchUnit "fail the build if" rules, and both hallucination warnings (Testcontainers 2.x, Jackson 3)
    - frontend file contains the zone chain, tokens-only rule, i18n parity rule, and src/generated/ rule
  </acceptance_criteria>
  <done>Tree constitutions live in place per D-15, inside budget, hallucination-hardened.</done>
</task>

<task type="auto">
  <name>Task 3: claude-md-check.yml — GATE-12 blocking in CI</name>
  <files>.github/workflows/claude-md-check.yml</files>
  <read_first>
    - .planning/phases/01-dogfood-bootstrap-enforcement/01-PATTERNS.md (claude-md-check workflow section)
    - scripts/checks/claude-md-check.mjs (CLI flags)
  </read_first>
  <action>
    Author `.github/workflows/claude-md-check.yml`: trigger push + pull_request, permissions contents: read. Job `claude-md-check` (ubuntu): checkout, run `node scripts/checks/claude-md-check.mjs --root-template templates/claude/ROOT-CLAUDE.template.md --tree-file backend/CLAUDE.md --tree-file frontend/CLAUDE.md --smoke-manifest templates/claude/smoke-commands.json --leg ubuntu` (installs go-task pinned first so `task --list`/`task up --dry` smoke runs). Add a CRLF guard step: fail if any tracked text file contains `\r\n` (inline node scan — Pitfall 6 continuous enforcement). Push, confirm green via `gh run watch`, then add `claude-md-check` to the required status checks configured in plan 01-09 (gh api ruleset update).
  </action>
  <verify>
    <automated>node -e "const y=require('fs').readFileSync('.github/workflows/claude-md-check.yml','utf8');for(const k of ['claude-md-check.mjs','--root-template','--smoke-manifest','--leg ubuntu'])if(!y.includes(k))throw 'missing: '+k;console.log('OK')" && gh run list --workflow=claude-md-check.yml --limit 1</automated>
  </verify>
  <acceptance_criteria>
    - Latest claude-md-check run green on GitHub
    - Workflow runs the real check CLI on template + both tree files + manifest; CRLF guard step present
    - `claude-md-check` listed among required status checks (gh api read-back recorded in SUMMARY)
  </acceptance_criteria>
  <done>Success criterion 2's CLAUDE.md half holds: size budgets and command smoke block merges (GATE-12).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLAUDE.md content → every agent session | Constitution files steer agent behavior; tampering = prompt-level attack surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-31 | Tampering | Constitution files edited without review | mitigate | backend/frontend CLAUDE.md + templates/claude/** are T3 (tiers.json) + CODEOWNERS; CLAUDE.md edits go through PR review like code (CLAUDE-CODE-RUNTIME §1) |
| T-01-32 | Tampering | Smoke manifest weakened to test nothing | mitigate | runSmoke fails on zero declared commands (01-07 test-pinned); manifest is T3-pathed |
| T-01-SC | Tampering | package installs | accept | Zero npm dependencies |
</threat_model>

<verification>
- claude-md-check workflow green on GitHub and required
- Local check CLI green on template + tree files
</verification>

<success_criteria>
- AGENT-01: three-layer CLAUDE.md within budgets (root as render-checked source per D-13/D-14, tree files in place per D-15)
- GATE-12 blocking in CI with non-vacuous smoke
</success_criteria>

<output>
Create `.planning/phases/01-dogfood-bootstrap-enforcement/01-10-SUMMARY.md` when done.
</output>
