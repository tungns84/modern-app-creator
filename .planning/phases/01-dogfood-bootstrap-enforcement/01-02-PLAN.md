---
phase: 01-dogfood-bootstrap-enforcement
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/spikes/q002-hosting-api.md
  - .planning/spikes/q004-verify-fast.md
autonomous: true
requirements: [GATE-10, AGENT-09]
must_haves:
  truths:
    - "Q-002 spike report answers empirically how GitHub PR-review API + rulesets behave for: self-approval, approve-then-push (stale), bot reviews, pull_request_review re-trigger of a required check, merge-method restriction"
    - "Q-002 report states the locked division: platform (ruleset/CODEOWNERS) enforces approver identity; CI re-verifies via reviews API (D-02), and decides commit-binding strictness"
    - "Q-004 spike report defines the verify --fast gate-set CONTRACT (static-only, no containers, D-19) and the per-gate timing output format"
    - "Both reports live in .planning/spikes/ (meta, never ships — D-07)"
  artifacts:
    - path: ".planning/spikes/q002-hosting-api.md"
      provides: "GATE-10 mechanism empirics (assumptions A5, A6, A10 resolved)"
      contains: "pull_request_review"
    - path: ".planning/spikes/q004-verify-fast.md"
      provides: "verify --fast contract + timing harness spec (D-19)"
      contains: "gate-set"
  key_links:
    - from: ".planning/spikes/q002-hosting-api.md"
      to: "scripts/checks/plan-compliance.mjs (plan 01-09)"
      via: "spike findings dictate gate trigger events + review-reduction rules"
      pattern: "APPROVED"
    - from: ".planning/spikes/q004-verify-fast.md"
      to: "Taskfile.yml verify/verify:fast tasks (plan 01-11)"
      via: "gate-set contract consumed when wiring task verify"
      pattern: "verify"
---

<objective>
Run two of the four upfront spikes (D-20): Q-002 (GitHub hosting-API approver-identity mechanism — the empirical foundation of GATE-10/D-02) and Q-004 (verify-fast gate-set contract — pre-directed by D-19). Spike reports are meta artifacts in `.planning/spikes/` (D-07); the throwaway test repo never touches this tree.

Purpose: Plan 01-09 (plan-compliance gate) must not be built on assumptions A5/A6/A10 — Pitfall 4 (stuck-red required check) and Pitfall 3 (stale reviews) are exactly the failure modes the spike de-risks. Q-004 prevents Phase 2 from inheriting an undefined verify --fast commitment.
Output: Two spike reports with explicit exit-criteria verdicts.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-dogfood-bootstrap-enforcement/01-CONTEXT.md
@.planning/phases/01-dogfood-bootstrap-enforcement/01-RESEARCH.md
</context>

## Artifacts this phase produces (this plan)

- New file paths: `.planning/spikes/q002-hosting-api.md`, `.planning/spikes/q004-verify-fast.md`
- New decisions recorded: commit-binding strictness for review approval (strict head-SHA vs latest-per-user only); ruleset-vs-classic-branch-protection availability on this repo's plan/visibility; verify --fast gate-set membership list

<tasks>

<task type="auto">
  <name>Task 1: Q-002 spike — GitHub PR-review mechanism empirics in a throwaway repo</name>
  <files>.planning/spikes/q002-hosting-api.md</files>
  <read_first>
    - .planning/phases/01-dogfood-bootstrap-enforcement/01-RESEARCH.md (Pattern 3 algorithm steps 1-8; Pitfalls 3, 4, 5; Open Question 2; Assumptions A5, A6, A10)
    - .planning/phases/01-dogfood-bootstrap-enforcement/01-CONTEXT.md (D-01, D-02, D-03, D-10)
  </read_first>
  <action>
    Create a throwaway private repo via `gh repo create <login>/q002-spike --private` and script the PR scenarios with `gh` (D-01 narrows scope to GitHub.com). Scenarios to execute and record verbatim API responses for:
    1. Author attempts to approve own PR → expect platform rejection (resolves A10).
    2. Reviews API shape: `gh api repos/<slug>/pulls/<n>/reviews --paginate` — record observed `state` values, `user.type` for bot-context, `submitted_at` ordering (resolves A5). If no second human account is available, record which scenarios were verified live vs reconstructed from documented API responses — label the latter [Unverified] in the report; the gate's unit fixtures (plan 01-09) cover the logic layer regardless.
    3. Required-check re-trigger: define a trivial workflow with `on: pull_request` + `on: pull_request_review [submitted, dismissed]`, mark it required, verify a review submission re-runs it AND the run satisfies the same required-check context on the head SHA (resolves A6 / Pitfall 4 — record exact check-name matching behavior).
    4. Ruleset coverage: create a ruleset with require-PR, required status check, require code-owner review, dismiss-stale-approvals, merge-commit-only (D-03). Record whether rulesets (or which subset) are available on this account plan + private visibility; if limited, record the classic-branch-protection fallback mapping per RESEARCH State-of-the-Art.
    5. Decide commit-binding strictness (Pitfall 3): with dismiss-stale enabled, document whether the CI gate additionally asserts `commit_id == head SHA` — record the decision and rationale in the report.

    Write `.planning/spikes/q002-hosting-api.md` with: scenario table (scenario, expected, observed, verdict), resolved assumptions A5/A6/A10, the locked platform-vs-CI division (CODEOWNERS parsing stays platform-side per Don't-Hand-Roll), the commit-binding decision, and exact `on:` trigger block + `--jq` reduction expression for plan 01-09 to copy. Delete the throwaway repo afterwards (`gh repo delete --yes`).
  </action>
  <verify>
    <automated>node -e "const r=require('fs').readFileSync('.planning/spikes/q002-hosting-api.md','utf8');for(const k of ['pull_request_review','APPROVED','dismiss','merge','A5','A6'])if(!r.includes(k))throw 'missing: '+k;console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - Report contains a scenario table with ≥5 rows each carrying an observed-behavior verdict
    - Assumptions A5, A6, A10 each have an explicit RESOLVED/UNRESOLVED status line
    - Report contains the exact workflow `on:` trigger block and the reviews-reduction `--jq` expression plan 01-09 will copy
    - Report states the commit-binding decision (strict head-SHA: yes/no + rationale)
    - Throwaway repo deleted (gh repo view exits non-zero)
  </acceptance_criteria>
  <done>GATE-10's mechanism is empirically grounded; plan 01-09 can implement Pattern 3 without unverified assumptions (D-02).</done>
</task>

<task type="auto">
  <name>Task 2: Q-004 spike — verify-fast gate-set contract and timing format</name>
  <files>.planning/spikes/q004-verify-fast.md</files>
  <read_first>
    - .planning/phases/01-dogfood-bootstrap-enforcement/01-RESEARCH.md (Open Question 3; D-19 in user constraints; Validation Architecture sampling table)
    - .planning/ROADMAP.md (Phase 2 success criterion 5 — verify --fast <60s consumer)
  </read_first>
  <action>
    Write the gate-set CONTRACT document (D-19 pre-directs: fast = static-only, no containers, no Testcontainers). Do NOT benchmark Maven that does not exist yet (RESEARCH recommendation). The report defines:
    - `verify --fast` membership for Phase 1: node:test suites (hooks + checks), claude-md-check (render + budgets), settings-lint, skills-lint, meta-link-lint, `docker compose -f infra/compose.yaml config -q`, plan-compliance fixture tests. Growth rule: Phase 2 adds compile + ArchUnit + Modulith verify + pure unit tests; anything needing Docker/Testcontainers is `verify --full` only.
    - `verify --full` membership: everything in fast + `task up` health smoke + init-parity local run.
    - Timing output format the Taskfile `verify` task (plan 01-11) must implement: one line per gate `GATE <name> <millis>ms <PASS|FAIL>` plus a total line; budget assertion: fast total <60s, with Phase-1 baseline expected <10s.
    - Divergence measurement plan: fast-vs-full divergence (<5%) is measured in Phase 8 — state the metric definition now (per-rule failure sets compared over the phase's CI history).
    Record the Phase-1 baseline timings by running the currently existing checks if any are already merged at execution time; otherwise mark baseline "to be captured by plan 01-11" — do not block the spike on later plans.
  </action>
  <verify>
    <automated>node -e "const r=require('fs').readFileSync('.planning/spikes/q004-verify-fast.md','utf8');for(const k of ['gate-set','--fast','--full','60s','PASS|FAIL'])if(!r.includes(k))throw 'missing: '+k;console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - Report lists explicit fast vs full gate-set membership tables (no gate unassigned)
    - Timing line format `GATE <name> <millis>ms <PASS|FAIL>` specified verbatim
    - Growth rule for Phases 2-8 stated; divergence metric defined
  </acceptance_criteria>
  <done>verify --fast has a written contract before any Taskfile verify wiring exists (D-19); Phase 2 inherits a defined budget, not a vibe (AGENT-08 cross-phase note).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| gh CLI → GitHub API (authenticated) | Spike scripts act with the developer's token on a throwaway repo |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-04 | Spoofing | Bot review counted as approval | mitigate | Scenario 2 records `user.type` discriminator; gate (01-09) excludes Bot — spike confirms the field |
| T-01-05 | Tampering | Approve-then-push stale approval | mitigate | Scenario 4 verifies dismiss-stale-approvals; commit-binding decision recorded |
| T-01-06 | Information disclosure | Spike repo leaking real code | mitigate | Throwaway repo contains only synthetic fixture files; deleted at end |
| T-01-SC | Tampering | package installs | accept | No package installs — gh + node only |
</threat_model>

<verification>
- Both reports exist with all exit-criteria keys present (node -e assertions)
- Q-002 scenario table verdicts cover A5/A6/A10; Q-004 defines both gate sets and the timing format
</verification>

<success_criteria>
- Plan 01-09 can copy trigger block + jq reduction directly from the Q-002 report (zero unverified API assumptions)
- Plan 01-11 can wire `task verify`/`verify:fast` directly from the Q-004 contract
- Roadmap success criterion 5 (spike reports written before dependent phases commit) satisfied for Q-002/Q-004
</success_criteria>

<output>
Create `.planning/phases/01-dogfood-bootstrap-enforcement/01-02-SUMMARY.md` when done.
</output>
