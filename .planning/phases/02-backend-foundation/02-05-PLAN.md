---
phase: 02-backend-foundation
plan: 05
type: tdd
wave: 5
depends_on: ["02-04"]
files_modified:
  - backend/src/main/java/com/acme/app/shared/events/BoundedRetryListenerSupport.java
  - backend/src/main/java/com/acme/app/observability/EventPublicationMetrics.java
  - backend/src/main/resources/application.yml
  - backend/src/main/resources/db/migration/shared/V2__side_effect.sql
  - backend/src/test/java/com/acme/app/events/SampleEventFixtures.java
  - backend/src/test/java/com/acme/app/events/SampleEventListener.java
  - backend/src/test/java/com/acme/app/observability/KillListenerTest.java
  - backend/src/test/java/com/acme/app/events/BoundedRetryTest.java
  - backend/src/test/resources/application-test.yml
  - scripts/checks/run-gate.mjs
  - Taskfile.yml
  - specs/013-event-registry-kill-listener/spec.md
  - specs/013-event-registry-kill-listener/plan.md
autonomous: false
requirements: [FOUND-03, FOUND-05, AGENT-08]

must_haves:
  truths:
    - "The kill-listener test proves NO EVENT LOSS: a listener that dies mid-processing leaves the publication INCOMPLETE and the event is redelivered on republish/restart"
    - "The kill-listener test proves NO DOUBLE-EFFECT: after redelivery the side-effect table has exactly 1 row (idempotent listener via natural key / check-before-write, D-11)"
    - "Retries are bounded (never infinite, D-12); past the bound the publication stays INCOMPLETE, is logged, and increments an alerting metric — no silent drop"
    - "The flagship kill-listener proof runs against real PostgreSQL 16 by REUSING Plan 01's `PostgresIntegrationTest` (@ServiceConnection PG16) base fixture (FOUND-05) — Flyway applies the side-effect migration on that container"
    - "`verify --full` runs the Testcontainers + kill-listener gates; `verify --fast` (compile + ArchUnit + Modulith verify + pure unit) completes under 60s (AGENT-08 / success criterion 5)"
  artifacts:
    - path: "backend/src/test/java/com/acme/app/observability/KillListenerTest.java"
      provides: "no-loss + no-double-effect proof against real PG by extending Plan 01's PostgresIntegrationTest"
      contains: "PostgresIntegrationTest"
    - path: "backend/src/main/java/com/acme/app/shared/events/BoundedRetryListenerSupport.java"
      provides: "bounded-retry support (listener-side counter, Q-2 Option A) reading EventRetryProperties"
    - path: "backend/src/main/java/com/acme/app/observability/EventPublicationMetrics.java"
      provides: "alerting metric incremented when retry bound is exceeded (D-12)"
    - path: "backend/src/test/java/com/acme/app/events/BoundedRetryTest.java"
      provides: "proof retry stops at maxAttempts and publication stays INCOMPLETE + metric fires"
    - path: "backend/src/main/resources/db/migration/shared/V2__side_effect.sql"
      provides: "side_effect table migration (per-module Flyway, real-PG target for the idempotency proof)"
  key_links:
    - from: "backend/src/test/java/com/acme/app/observability/KillListenerTest.java"
      to: "backend/src/test/java/com/acme/app/PostgresIntegrationTest.java"
      via: "extends Plan 01's PG16 @ServiceConnection base fixture (FOUND-05)"
      pattern: "PostgresIntegrationTest"
    - from: "backend/src/main/java/com/acme/app/shared/events/BoundedRetryListenerSupport.java"
      to: "backend/src/main/java/com/acme/app/appconfig/EventRetryProperties.java"
      via: "reads maxAttempts/backoff from appconfig::spi"
      pattern: "EventRetryProperties"
    - from: "scripts/checks/run-gate.mjs"
      to: "backend/src/test/java/com/acme/app/observability/KillListenerTest.java"
      via: "kill-listener-test gate registered as --full only"
      pattern: "kill-listener"
---

<objective>
Build the phase's flagship proof (FOUND-03, success criterion 2): a kill-listener integration test against real PostgreSQL 16 — reusing Plan 01's Wave-0 `PostgresIntegrationTest` (Testcontainers 2.x, `@ServiceConnection`) base fixture — that demonstrates BOTH no event loss (a listener dying mid-processing leaves the JDBC Event Publication Registry record INCOMPLETE and the event is redelivered on republish) AND no double-effect (after redelivery the side-effect table holds exactly one row — idempotent listener, D-11). Add bounded-retry support (D-12 / Q-2 Option A: listener-side attempt counter reading `EventRetryProperties` from appconfig::spi) so retries never run infinitely; past the bound the publication stays INCOMPLETE, is logged, and increments an alerting metric — no silent drop. Finally, register the `--full`-only container gates (`backend-integration`, `kill-listener-test`) and the `--fast` `backend-unit` gate in `run-gate.mjs`, and prove `verify --fast` stays under the 60s budget (AGENT-08 / success criterion 5).

Purpose: This is the load-bearing event-durability guarantee the whole event-driven architecture rests on, plus the final piece of the Q-004 fast/full gate split. The real-PG harness already exists (Plan 01 `PostgresIntegrationTest`); this plan extends it with the side-effect table + the durability assertions. TDD is mandatory here — the failing assertions ARE the deliverable.
Output: bounded-retry support + metric; kill-listener + bounded-retry tests extending the Wave-0 PG fixture; side-effect Flyway migration; full/fast gate wiring; <60s fast-budget proof.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/02-backend-foundation/02-CONTEXT.md
@.planning/phases/02-backend-foundation/02-RESEARCH.md
@.planning/phases/02-backend-foundation/02-PATTERNS.md
@backend/CLAUDE.md
@.cowork/tiers.json
@.planning/spikes/q004-verify-fast.md
@scripts/checks/run-gate.mjs
@Taskfile.yml
@infra/compose.yaml
@.claude/skills/plan/SKILL.md
@.planning/phases/02-backend-foundation/02-01-SUMMARY.md
@.planning/phases/02-backend-foundation/02-02-SUMMARY.md
@.planning/phases/02-backend-foundation/02-04-SUMMARY.md
</context>

<feature>
  <name>Feature: loss-proof, double-effect-proof, bounded-retry JDBC Event Publication Registry</name>
  <files>
    backend/src/main/java/com/acme/app/shared/events/BoundedRetryListenerSupport.java,
    backend/src/main/java/com/acme/app/observability/EventPublicationMetrics.java,
    backend/src/main/resources/db/migration/shared/V2__side_effect.sql,
    backend/src/test/java/com/acme/app/events/SampleEventFixtures.java,
    backend/src/test/java/com/acme/app/events/SampleEventListener.java,
    backend/src/test/java/com/acme/app/observability/KillListenerTest.java,
    backend/src/test/java/com/acme/app/events/BoundedRetryTest.java
  </files>
  <behavior>
    RED→GREEN→REFACTOR, one failing assertion authored before each production change:
    - no-loss: publish an event whose `@TransactionalEventListener(phase=AFTER_COMMIT)` listener throws before completing → EVENT_PUBLICATION row stays INCOMPLETE; trigger republish-outstanding → listener re-runs → row becomes COMPLETED.
    - no-double-effect: the listener performs an idempotent side-effect (natural-key insert / check-before-write); after redelivery the side-effect table has exactly 1 row, not 2.
    - bounded retry: with `maxAttempts` from `EventRetryProperties`, a perpetually-failing listener is retried at most `maxAttempts` times; afterwards the publication stays INCOMPLETE, an alerting metric (`EventPublicationMetrics`) is incremented, and a log line is emitted — never an infinite loop, never a silent drop.
  </behavior>
  <implementation>Listener-side bounded-retry support (Q-2 Option A) + idempotent sample listener; Modulith JDBC EPR (v2 schema, never use-legacy-structure) with republish-outstanding-events-on-restart and staleness monitor already configured in Plan 04. Tests EXTEND Plan 01's Testcontainers 2.x `@ServiceConnection` PG16 base fixture (`PostgresIntegrationTest`); never reintroduce 1.x `@DynamicPropertySource`.</implementation>
</feature>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Side-effect migration + idempotent sample listener + bounded-retry support, on the Wave-0 PG fixture (RED→GREEN)</name>
  <files>backend/src/main/resources/db/migration/shared/V2__side_effect.sql, backend/src/test/java/com/acme/app/events/SampleEventFixtures.java, backend/src/test/java/com/acme/app/events/SampleEventListener.java, backend/src/test/java/com/acme/app/events/BoundedRetryTest.java, backend/src/main/java/com/acme/app/shared/events/BoundedRetryListenerSupport.java, backend/src/main/java/com/acme/app/observability/EventPublicationMetrics.java, backend/src/main/resources/application.yml, backend/src/test/resources/application-test.yml, specs/013-event-registry-kill-listener/spec.md, specs/013-event-registry-kill-listener/plan.md</files>
  <read_first>
    - .planning/phases/02-backend-foundation/02-01-SUMMARY.md (the Plan-01 `PostgresIntegrationTest` base fixture + `db/migration/shared` layout this plan reuses — do NOT recreate the fixture)
    - .planning/phases/02-backend-foundation/02-RESEARCH.md (JDBC EPR Deep Dive — lifecycle, republish-on-restart, bounded retry Q-2 Option A; Pattern 6 kill-listener; Pitfall 1 Testcontainers 2.x; Pitfall 5 @TransactionalEventListener AFTER_COMMIT)
    - .planning/phases/02-backend-foundation/02-PATTERNS.md (KillListenerTest Testcontainers 2.x pattern; ServiceConnection; Jackson 3 imports; constructor injection)
    - .planning/phases/02-backend-foundation/02-CONTEXT.md (D-11 at-least-once + idempotent listeners; D-12 bounded retry + metric + no silent drop)
    - backend/src/main/java/com/acme/app/appconfig/EventRetryProperties.java (maxAttempts/backoff source)
    - backend/src/main/java/com/acme/app/shared/scheduling/ScheduledTask.java (no bare @Scheduled rule applies to any helper)
    - backend/src/test/java/com/acme/app/PostgresIntegrationTest.java (Plan 01 base fixture this plan's tests extend)
    - .claude/skills/plan/SKILL.md (bound-spec format)
  </read_first>
  <behavior>
    - The tests EXTEND Plan 01's `PostgresIntegrationTest` base (Testcontainers 2.x `@ServiceConnection` PG16) — they do NOT define a new container or use `@DynamicPropertySource`; Boot 4 autoconfigures the datasource; Flyway runs the per-module migrations (now incl. `V2__side_effect.sql`); the tests are tagged `integration` so they are `--full` only.
    - `SampleEventListener` applies an idempotent side-effect keyed by a natural key (check-before-write) so a redelivered event does not duplicate the effect.
    - `BoundedRetryListenerSupport` tracks per-event attempt count against `EventRetryProperties.maxAttempts()`; on exceeding it, it stops retrying, leaves the publication INCOMPLETE, increments `EventPublicationMetrics`, and logs — verified by `BoundedRetryTest`.
  </behavior>
  <action>
    Author bound spec `specs/013-event-registry-kill-listener/{spec,plan}.md` (tier T3 — touches package-info-adjacent shared/events, the shared Flyway migration, pom-managed Modulith config, run-gate.mjs + Taskfile; `Approved-by:` EMPTY); bind branch `feat/013-event-registry-kill-listener`. Add `backend/src/main/resources/db/migration/shared/V2__side_effect.sql` creating the `side_effect` table (natural-key unique constraint for idempotency) under the existing `shared` per-module Flyway location (the location list / `ModuleFlywayLocationsCustomizer` already covers `db/migration/shared` from Plans 01–02 — no new location needed). RED first: write a failing `BoundedRetryTest` expressing the bounded-retry behavior before the support class exists; it EXTENDS Plan 01's `PostgresIntegrationTest` (no new container). Create the test fixtures in a dedicated `com.acme.app.events` TEST package (events fixtures are test-only — they must not create a new production module or widen the DAG): `SampleEventFixtures` (a sample domain event record + a `side_effect` table accessor) and `SampleEventListener` (`@TransactionalEventListener(phase=AFTER_COMMIT)`, idempotent via natural-key check-before-write against the `side_effect` table; a test toggle makes it throw to simulate listener death). GREEN: implement `shared/events/BoundedRetryListenerSupport.java` (Q-2 Option A — a per-event attempt counter; reads `EventRetryProperties` from appconfig::spi via constructor injection; exposes a guard the listener calls so retries stop at `maxAttempts`) and `observability/EventPublicationMetrics.java` (a Micrometer counter incremented when the bound is exceeded). Confirm the Modulith 2.0.6 republish/outstanding-publications API surface during implementation (RESEARCH names it [ASSUMED]); use the actual API and record any correction. Ensure `application.yml` EPR config is the v2 schema (`schema-initialization.enabled=true`, NEVER `use-legacy-structure`) with `republish-outstanding-events-on-restart=true` and the staleness monitor (carried over from Plan 04).
  </action>
  <verify>
    <automated>./mvnw -q verify -Dgroups=integration -Dtest=BoundedRetryTest -f backend/pom.xml</automated>
  </verify>
  <acceptance_criteria>
    - `BoundedRetryTest` EXTENDS Plan 01's `PostgresIntegrationTest` (`@ServiceConnection` PG16; no `@DynamicPropertySource`, no 1.x idioms) and Flyway applies `V2__side_effect.sql` against the real container.
    - `BoundedRetryTest` proves a perpetually-failing listener is retried at most `maxAttempts` times, then the publication stays INCOMPLETE, the `EventPublicationMetrics` counter increments, and a log line is emitted — no infinite loop, no silent drop.
    - `BoundedRetryListenerSupport` reads `maxAttempts` from the appconfig::spi `EventRetryProperties` record (no `@Value`).
    - Event fixtures live in a test package only — `ModulithVerifyTest` name set is unchanged (`{shared, appconfig, i18n, caching, observability}`).
  </acceptance_criteria>
  <done>Side-effect migration + idempotent sample listener + bounded-retry support exist and pass on the reused Wave-0 PG fixture, with the retry bound proven.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: KillListenerTest — no-loss + no-double-effect proof (RED→GREEN→REFACTOR)</name>
  <files>backend/src/test/java/com/acme/app/observability/KillListenerTest.java</files>
  <read_first>
    - .planning/phases/02-backend-foundation/02-RESEARCH.md (Pattern 6 kill-listener six-step outline; EPR lifecycle state machine; republish-on-restart; Pitfall 5 AFTER_COMMIT)
    - .planning/phases/02-backend-foundation/02-PATTERNS.md (KillListenerTest pattern; Testcontainers 2.x ServiceConnection; constructor injection)
    - .planning/phases/02-backend-foundation/02-CONTEXT.md (specifics: flagship proof must show BOTH no-loss AND no-double-effect, not just redelivery)
    - backend/src/test/java/com/acme/app/events/SampleEventListener.java (idempotent listener from Task 1)
    - backend/src/test/java/com/acme/app/PostgresIntegrationTest.java (Plan 01 base fixture this test extends)
  </read_first>
  <behavior>
    - `eventNotLostOnListenerDeath`: publish event → listener throws before completing → assert the EVENT_PUBLICATION row is INCOMPLETE → trigger republish-outstanding (simulate restart) → assert the row becomes COMPLETED (redelivery proven = no loss).
    - `sideEffectAppliedExactlyOnceAfterRedelivery`: after the redelivery above, assert the `side_effect` table has exactly 1 row (idempotency proven = no double-effect).
  </behavior>
  <action>
    RED: author both `KillListenerTest` methods as failing assertions first (querying EVENT_PUBLICATION state and the side_effect row count), extending Plan 01's `PostgresIntegrationTest`, tagged `integration` (`--full` only). Drive listener death via the Task-1 `SampleEventListener` throw toggle. For the "restart" step, invoke the Modulith republish-outstanding-publications API directly (confirm the exact 2.0.6 method during implementation) rather than literally restarting the JVM. GREEN: ensure the production wiring (idempotent listener + EPR config + bounded-retry support) makes both assertions pass — the side-effect count must be exactly 1 after redelivery (natural-key idempotency), and the publication must transition INCOMPLETE→COMPLETED. REFACTOR: extract any shared query helpers into `PostgresIntegrationTest` or a small test util; keep the two assertions crisp. Do NOT weaken the assertions to get green — if the side-effect appears twice, fix the idempotency, never the test.
  </action>
  <verify>
    <automated>./mvnw -q verify -Dtest=KillListenerTest -f backend/pom.xml</automated>
  </verify>
  <acceptance_criteria>
    - `KillListenerTest.eventNotLostOnListenerDeath` proves INCOMPLETE-then-redelivered-to-COMPLETED (no loss).
    - `KillListenerTest.sideEffectAppliedExactlyOnceAfterRedelivery` proves exactly 1 side-effect row after redelivery (no double-effect).
    - The test EXTENDS Plan 01's `PostgresIntegrationTest` (Testcontainers 2.x `@ServiceConnection` PG16); no `@DynamicPropertySource`; no test-weakening assertions.
  </acceptance_criteria>
  <done>FOUND-03 flagship proof is green: the JDBC Event Publication Registry loses no events and applies side-effects exactly once.</done>
</task>

<task type="auto">
  <name>Task 3: Wire full/fast gate split + prove verify --fast < 60s (AGENT-08)</name>
  <files>scripts/checks/run-gate.mjs, Taskfile.yml</files>
  <read_first>
    - scripts/checks/run-gate.mjs (the gates()/mode refactor from Plan 01 — add backend-unit fast + backend-integration/kill-listener-test full)
    - .planning/spikes/q004-verify-fast.md (§7 Phase 2 growth rule; §5 timing format; §2.6 full = strict superset of fast; budget = gate)
    - Taskfile.yml (verify / verify:fast tasks)
    - .planning/phases/02-backend-foundation/02-RESEARCH.md (verify --fast / --full gate-set tables)
    - .planning/phases/02-backend-foundation/02-PATTERNS.md (run-gate.mjs extension snippet — fast/full entries)
  </read_first>
  <action>
    Extend `run-gate.mjs` gate list (the `mode`-aware `gates()` from Plan 01) with the remaining Phase 2 gates: `backend-unit` (`./mvnw -q test -Dgroups=unit -f backend/pom.xml`, `fast:true`); `backend-integration` (`./mvnw -q verify -Dgroups=integration -f backend/pom.xml`, `fast:false` — this now includes Plan 01's `SharedFlywaySchemaTest` + Plan 05's PG-backed tests); `kill-listener-test` (`./mvnw -q verify -Dtest=KillListenerTest -f backend/pom.xml`, `fast:false`). Confirm `--full` is a strict superset of `--fast` (Q-004 §2.6) and the budget-is-a-gate logic still fails the run if fast TOTAL ≥ 60000ms. Keep gate failure output in the AGENT-08 rule+fix format. Update the `Taskfile.yml` comment block to reflect the now-realized Phase 2 fast/full divergence (no structural task change needed — both tasks still delegate to run-gate.mjs). run-gate.mjs and Taskfile.yml are T3 — covered by the Task-1 bound spec. Then RUN `task verify:fast` and capture the verbatim `GATE`/`TOTAL` output; record it in the SUMMARY and confirm TOTAL < 60000ms (success criterion 5). If the fast TOTAL exceeds budget, move the offending gate to `--full` via this same T3 change (never raise the budget) and re-capture.
  </action>
  <verify>
    <automated>node scripts/checks/run-gate.mjs --mode fast</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/checks/run-gate.mjs --mode fast` prints `backend-compile`, `archunit`, `modulith-verify`, `backend-unit` (plus Phase 1 gates) with the `GATE <name> <millis>ms <PASS|FAIL>` format and a `TOTAL <millis>ms PASS` line with TOTAL < 60000ms.
    - `node scripts/checks/run-gate.mjs --mode full` additionally runs `backend-integration` (incl. `SharedFlywaySchemaTest`) and `kill-listener-test` (strict superset).
    - A failing gate emits a rule-named, fix-bearing block (AGENT-08).
    - The captured fast-mode TOTAL is recorded in the SUMMARY as the Phase 2 baseline.
  </acceptance_criteria>
  <done>The Q-004 fast/full split is fully realized for Phase 2; `verify --fast` is proven under 60s; container-class gates are full-only.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: H2 approval — EPR durability proof + gate split</name>
  <files>specs/013-event-registry-kill-listener/plan.md</files>
  <action>Blocking human-verify checkpoint: present the built artifacts for review and pause for H2 approval; the executor performs no further writes until the resume signal. See <what-built>/<how-to-verify>/<resume-signal> below.</action>
  <what-built>
    The JDBC Event Publication Registry durability proof (no-loss + no-double-effect kill-listener test) extending Plan 01's real-PG Testcontainers fixture (FOUND-05), bounded-retry support with an alerting metric (D-12), the side-effect Flyway migration, and the completed Q-004 fast/full gate split with a measured `verify --fast` under 60s. run-gate.mjs, Taskfile.yml, and the shared/events support are T3 — this checkpoint is the H2 approval for `specs/013-event-registry-kill-listener/plan.md`.
  </what-built>
  <how-to-verify>
    1. Ensure a container runtime is up (`docker ps`) or rely on Testcontainers; run `./mvnw -q verify -f backend/pom.xml` — the full backend suite (incl. KillListenerTest, BoundedRetryTest, SharedFlywaySchemaTest) passes.
    2. Run `node scripts/checks/run-gate.mjs --mode fast` — confirm the TOTAL line is PASS and < 60000ms; note the value.
    3. Run `node scripts/checks/run-gate.mjs --mode full` — confirm `kill-listener-test` and `backend-integration` run and pass.
    4. Read `KillListenerTest` — confirm it extends `PostgresIntegrationTest` and asserts BOTH the INCOMPLETE→COMPLETED transition AND exactly-one side-effect row (not just redelivery).
    5. Review `specs/013-event-registry-kill-listener/plan.md`. To APPROVE, fill `Approved-by:` yourself via the GitHub PR review (H2) — never let the agent fill it.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to fix.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| event publisher → listener (async, AFTER_COMMIT) | events cross module boundaries via the registry; at-least-once delivery means a listener may see the same event twice |
| EVENT_PUBLICATION table (PostgreSQL) | the durable record of in-flight events; its integrity is the no-loss guarantee |
| build → main branch | shared/events support + shared Flyway migration + gate config (run-gate.mjs, Taskfile.yml) are T3 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-14 | Tampering (integrity) | Double-effect: at-least-once redelivery applies a side-effect twice (replay / double-spend class) | mitigate | Mandatory idempotent listeners (D-11, natural-key check-before-write); `KillListenerTest.sideEffectAppliedExactlyOnceAfterRedelivery` proves exactly-once effect |
| T-02-15 | Denial of service | Infinite retry of a poison event saturating the registry/scheduler | mitigate | Bounded retry (D-12) capped at `maxAttempts`; past the bound the publication stays INCOMPLETE + metric + log; `BoundedRetryTest` proves the cap |
| T-02-16 | Repudiation / availability | Silent event drop hides a failed side-effect (no audit, no alert) | mitigate | No silent drop: exhausted retries leave the record INCOMPLETE, emit a log line, and increment `EventPublicationMetrics` (alertable); staleness monitor flags stuck publications |
| T-02-17 | Tampering | Event loss if listener runs in the publisher TX and the TX rolls back | mitigate | `@TransactionalEventListener(phase=AFTER_COMMIT)` (Pitfall 5) ensures the listener runs only after commit; the registry persists INCOMPLETE until completion |
| T-02-SC | Tampering | npm/Maven installs (no new external deps — Modulith starters + Testcontainers already in pom; Micrometer Boot-managed) | mitigate | No new packages; all artifacts from the Boot/Modulith BOMs pinned in Plan 01; bound spec reviewed at H2 |
</threat_model>

<verification>
- `./mvnw -q verify -f backend/pom.xml` passes the full backend suite (KillListenerTest, BoundedRetryTest, SharedFlywaySchemaTest, all module + gate tests).
- `node scripts/checks/run-gate.mjs --mode fast` → TOTAL PASS, < 60000ms.
- `node scripts/checks/run-gate.mjs --mode full` runs the container-class gates (strict superset).
</verification>

<success_criteria>
- FOUND-03 proven: no event loss AND no double-effect through the JDBC EPR (kill-listener test).
- Bounded retry (D-12): capped attempts, INCOMPLETE-after-bound, alerting metric, no silent drop — proven by test.
- FOUND-05: integration tests on real PostgreSQL 16 via Testcontainers 2.x `@ServiceConnection` (reusing Plan 01's Wave-0 base fixture); Flyway per-module migrations (incl. `V2__side_effect.sql`).
- AGENT-08 / success criterion 5: `verify --fast` (compile + ArchUnit + Modulith verify + pure unit) under 60s, with full as a strict superset; gate errors name rule + fix.
</success_criteria>

<artifacts_this_phase_produces>
- Production: `shared.events.BoundedRetryListenerSupport` (Q-2 Option A bounded-retry guard, reads `EventRetryProperties`); `observability.EventPublicationMetrics` (Micrometer counter for exceeded-bound alerting, D-12).
- Migration: `db/migration/shared/V2__side_effect.sql` (idempotency target table; per-module Flyway location already owned by Plans 01–02).
- Config: `application.yml` EPR v2 schema + republish-outstanding-events-on-restart + staleness monitor confirmed; `application-test.yml` integration profile.
- Tests: `events.SampleEventFixtures`, `events.SampleEventListener` (idempotent, AFTER_COMMIT), `BoundedRetryTest`, `observability.KillListenerTest` (no-loss + no-double-effect) — all extending Plan 01's `PostgresIntegrationTest`.
- Gate runner: `run-gate.mjs` gains `backend-unit` (fast), `backend-integration` (full), `kill-listener-test` (full); Taskfile.yml comment updated.
- Spec: `specs/013-event-registry-kill-listener/{spec,plan}.md`.
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/02-backend-foundation/02-05-SUMMARY.md` when done.
</output>
