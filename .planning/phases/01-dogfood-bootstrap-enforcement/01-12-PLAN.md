---
phase: 01-dogfood-bootstrap-enforcement
plan: 12
type: execute
wave: 1
depends_on: []
files_modified:
  - specs/001-hooks-enforcement/spec.md
  - specs/001-hooks-enforcement/plan.md
  - specs/002-plan-compliance-ci/spec.md
  - specs/002-plan-compliance-ci/plan.md
  - specs/003-local-stack/spec.md
  - specs/003-local-stack/plan.md
  - specs/004-init-script/spec.md
  - specs/004-init-script/plan.md
  - specs/005-claude-pack/spec.md
  - specs/005-claude-pack/plan.md
  - specs/006-adr-foundation/spec.md
  - specs/006-adr-foundation/plan.md
autonomous: true
requirements: [AGENT-06, AGENT-09]
must_haves:
  truths:
    - "Six hand-written spec units (specs/001..006) exist with spec.md + plan.md, tier: T3, and an Approved-by audit line — manual dogfood stage of the ratchet (D-05)"
    - "No spec unit links into .planning/ (D-06/D-07 — specs ship in the template)"
    - "Every spec.md cites at least one Phase-1 requirement ID"
  artifacts:
    - path: "specs/001-hooks-enforcement/plan.md"
      provides: "First manual dogfood spec unit (D-05/D-09)"
      contains: "Approved-by:"
    - path: "specs/002-plan-compliance-ci/plan.md"
      provides: "Spec unit consumed by the CI gate plan 01-09"
      contains: "tier: T3"
    - path: "specs/006-adr-foundation/plan.md"
      provides: "Spec unit for the ADR set (constitution change = T3 per AI-COWORK §2)"
      contains: "tier: T3"
  key_links:
    - from: "specs/00*/plan.md"
      to: "plan-compliance CI gate (plan 01-09) and T3 hook (plan 01-04)"
      via: "Approved-by audit line + tier declaration, bound per-change via the feat/NNN-* branch convention this plan establishes"
      pattern: "Approved-by:"
---

<objective>
Author the six hand-written spec units (specs/001..006) — the "manual dogfood" stage of the enforcement ratchet (AGENT-09): specs exist and are approved BEFORE any automation enforces them (D-05, D-06, D-09). Split out of plan 01-01 to keep both wave-1 plans within scope budget.

Purpose: The hooks (01-04) and the CI gate (01-09) verify the presence of the approved spec unit BOUND to each change (via the feat/NNN-* branch convention established here); this plan puts those units on record first.
Output: specs/001-006 directories with spec.md + plan.md each, plus the specs/NNN and branch-naming conventions.

Format note (AGENT-06 scope): AGENT-06 mentions `{spec,plan,tasks}.md`; per AI-COWORK §4 `tasks.md` is explicitly "optional: ordered task breakdown for large features" — D-05 (LOCKED) specifies spec.md + plan.md, so these units deliberately omit tasks.md (no requirement gap).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-dogfood-bootstrap-enforcement/01-CONTEXT.md
@.planning/phases/01-dogfood-bootstrap-enforcement/01-PATTERNS.md
@product/methodology/AI-COWORK.md
</context>

## Artifacts this phase produces (this plan)

- New file paths: `specs/001-hooks-enforcement/{spec,plan}.md`, `specs/002-plan-compliance-ci/{spec,plan}.md`, `specs/003-local-stack/{spec,plan}.md`, `specs/004-init-script/{spec,plan}.md`, `specs/005-claude-pack/{spec,plan}.md`, `specs/006-adr-foundation/{spec,plan}.md`
- New conventions: spec dir naming `specs/NNN-short-name/`, branch naming `feat/NNN-short-name` (load-bearing: both enforcement layers use the branch NNN to bind a change to its spec unit), commit REQ-ID citation rule
- Format convention: each unit ships `spec.md` + `plan.md`; `tasks.md` is optional per AI-COWORK §4 and omitted in Phase 1

<tasks>

<task type="auto">
  <name>Task 1: Spec units 001–003 (hooks, plan-compliance CI, local stack)</name>
  <files>specs/001-hooks-enforcement/spec.md, specs/001-hooks-enforcement/plan.md, specs/002-plan-compliance-ci/spec.md, specs/002-plan-compliance-ci/plan.md, specs/003-local-stack/spec.md, specs/003-local-stack/plan.md</files>
  <read_first>
    - product/methodology/AI-COWORK.md (§3.2 plan content, §4 spec artifact format — the authoritative format; note §4 marks tasks.md optional)
    - .planning/phases/01-dogfood-bootstrap-enforcement/01-PATTERNS.md (specs/NNN section — format excerpt and ratchet narrative)
    - .planning/phases/01-dogfood-bootstrap-enforcement/01-CONTEXT.md (D-05, D-09, D-10)
  </read_first>
  <action>
    Write three spec units in English (technical artifacts), numbering from 001 (`000-example` reserved for Phase 8 per D-09):
    - `001-hooks-enforcement` — covers `.cowork/tiers.json`, `.cowork/waivers.json`, `.claude/hooks/**`, `.claude/settings.json` (REQ: AGENT-03, AGENT-04)
    - `002-plan-compliance-ci` — covers `.github/workflows/plan-compliance.yml`, `.github/CODEOWNERS`, `scripts/checks/plan-compliance.mjs`, `scripts/checks/assert-non-author-approval.mjs`, repo ruleset/branch-protection config (REQ: GATE-10)
    - `003-local-stack` — covers `infra/compose.yaml`, `Taskfile.yml`, `.github/workflows/os-matrix.yml` (REQ: FOUND-02)

    Each `spec.md`: user story, acceptance criteria (lift from the matching plan's must_haves in this phase), REQ-IDs cited. Each `plan.md`: files to touch, modules affected (n/a pre-backend), tests, `tier: T3`, and the audit line `Approved-by: <github-login> 2026-06-11 (H2 approval granted via GSD Phase-1 plan review; solo self-approval waived per .cowork/waivers.json W-001)`. Per D-02 this line is audit trail, NOT authorization proof — state that sentence verbatim inside each plan.md. `tasks.md` is omitted per AI-COWORK §4 (optional).

    Specs must NOT link into `.planning/` (D-06/D-07 — they ship in the template; meta-link-lint in plan 01-11 will enforce this).
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const dirs=['001-hooks-enforcement','002-plan-compliance-ci','003-local-stack'];for(const d of dirs){const p=fs.readFileSync('specs/'+d+'/plan.md','utf8');if(!/tier:\s*T3/.test(p))throw d+' missing tier';if(!/Approved-by:\s*\S/.test(p))throw d+' missing approval';if(/\.planning\//.test(p))throw d+' links .planning';fs.accessSync('specs/'+d+'/spec.md')};console.log('OK 3 specs')"</automated>
  </verify>
  <acceptance_criteria>
    - 3 directories `specs/00[1-3]-*/` each contain `spec.md` and `plan.md`
    - Every plan.md contains the literal `tier: T3`, a non-empty `Approved-by:` line, and zero occurrences of the string `.planning/`
    - Every spec.md cites at least one REQ-ID from {AGENT-03, AGENT-04, GATE-10, FOUND-02}
  </acceptance_criteria>
  <done>The three spec units that gate the enforcement and stack plans (01-04, 01-05, 01-09) are on record.</done>
</task>

<task type="auto">
  <name>Task 2: Spec units 004–006 (init script, claude pack, ADR foundation)</name>
  <files>specs/004-init-script/spec.md, specs/004-init-script/plan.md, specs/005-claude-pack/spec.md, specs/005-claude-pack/plan.md, specs/006-adr-foundation/spec.md, specs/006-adr-foundation/plan.md</files>
  <read_first>
    - product/methodology/AI-COWORK.md (§3.2 plan content, §4 spec artifact format — same format as Task 1)
    - .planning/phases/01-dogfood-bootstrap-enforcement/01-CONTEXT.md (D-05, D-09, D-21)
  </read_first>
  <action>
    Write the remaining three spec units in the same format and with the same Approved-by audit-line rule as Task 1 (grouping adjusts D-09's five suggested units by adding 006 for the ADR set — planner-permitted grouping adjustment):
    - `004-init-script` — covers `scripts/init.sh`, `scripts/init.ps1`, `scripts/init-core.mjs`, `.github/workflows/init-parity.yml` (REQ: FOUND-12)
    - `005-claude-pack` — covers `templates/claude/**`, `backend/CLAUDE.md`, `frontend/CLAUDE.md`, `.claude/skills/**`, `scripts/checks/{render-claude-md,claude-md-check,settings-lint,skills-lint,meta-link-lint}.mjs`, `.github/workflows/claude-md-check.yml` (REQ: AGENT-01, AGENT-02, GATE-12)
    - `006-adr-foundation` — covers `docs/adr/0001..0003` (constitution change = T3 per AI-COWORK §2)
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const dirs=['001-hooks-enforcement','002-plan-compliance-ci','003-local-stack','004-init-script','005-claude-pack','006-adr-foundation'];for(const d of dirs){const p=fs.readFileSync('specs/'+d+'/plan.md','utf8');if(!/tier:\s*T3/.test(p))throw d+' missing tier';if(!/Approved-by:\s*\S/.test(p))throw d+' missing approval';if(/\.planning\//.test(p))throw d+' links .planning';fs.accessSync('specs/'+d+'/spec.md')};console.log('OK 6 specs')"</automated>
    <human-check>End-of-phase: developer confirms the six Approved-by lines reflect their real H2 approval of this phase plan set (dogfood integrity, D-05/D-10).</human-check>
  </verify>
  <acceptance_criteria>
    - All 6 directories `specs/00[1-6]-*/` each contain `spec.md` and `plan.md` (combined verify covers Task 1 output too)
    - Every plan.md contains the literal `tier: T3`, a non-empty `Approved-by:` line, and zero occurrences of the string `.planning/`
    - Every spec.md cites at least one REQ-ID from {AGENT-01..06, AGENT-09, GATE-10, GATE-12, FOUND-02, FOUND-12}
  </acceptance_criteria>
  <done>The manual-dogfood stage of the ratchet is complete: every T3 deliverable of Phase 1 has an approved spec unit BEFORE hooks or CI enforce anything (D-05; AGENT-06 — tasks.md optional per AI-COWORK §4; AGENT-09 evidence).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Approved-by line in specs/*/plan.md → gate verdicts | Both enforcement layers read this line; a forged line must never equal authorization |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-03 | Spoofing | Approved-by line in specs/*/plan.md | accept (by design) | D-02: line is audit-only; authorization is PR-review API + ruleset — sentence recorded verbatim in each plan.md |
| T-01-SC | Tampering | package installs | accept | No npm/pip/cargo installs (documentation-only plan) |
</threat_model>

<verification>
- `node -e` assertion over all 6 spec units passes (tier: T3, Approved-by present, zero .planning/ links)
</verification>

<success_criteria>
- specs/001..006 approved units on record before hooks/CI go live (D-05, AGENT-06, AGENT-09)
- Spec/branch/commit conventions documented for all subsequent T3 work (feat/NNN-* branch = the binding key both gates consume)
</success_criteria>

<output>
Create `.planning/phases/01-dogfood-bootstrap-enforcement/01-12-SUMMARY.md` when done.
</output>
