---
phase: 02-backend-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/pom.xml
  - backend/mvnw
  - backend/mvnw.cmd
  - backend/.mvn/wrapper/maven-wrapper.properties
  - backend/src/main/java/com/acme/app/Application.java
  - backend/src/main/java/com/acme/app/shared/package-info.java
  - backend/src/main/java/com/acme/app/shared/scheduling/ScheduledTask.java
  - backend/src/main/java/com/acme/app/shared/scheduling/ScheduledTaskRegistrar.java
  - backend/src/main/java/com/acme/app/shared/query/NativeQuery.java
  - backend/src/main/resources/application.yml
  - backend/src/test/resources/application-test.yml
  - backend/src/test/java/com/acme/app/ModulithVerifyTest.java
  - backend/src/test/java/com/acme/app/ArchitectureGatesTest.java
  - scripts/checks/run-gate.mjs
  - Taskfile.yml
  - specs/008-backend-skeleton-gates/spec.md
  - specs/008-backend-skeleton-gates/plan.md
  - .planning/spikes/q1-spring-cloud-consul-compat.md
autonomous: false
requirements: [FOUND-01, FOUND-05, GATE-01, GATE-02, AGENT-08]
user_setup:
  - service: JDK 25 Temurin
    why: "Backend compile + all Maven gates require JDK 25 LTS (class file 69); no fallback"
    dashboard_config:
      - task: "Install Temurin 25 JDK and ensure `java -version` reports 25"
        location: "winget install EclipseAdoptium.Temurin.25.JDK (Windows) / sdkman or brew (macOS/Linux)"

must_haves:
  truths:
    - "`./mvnw verify` runs Modulith verify + ArchUnit gates against a green skeleton"
    - "An undeclared module dependency or DAG cycle fails the Modulith verify gate"
    - "A field-injection, bare @Scheduled, unwrapped native query, @Transactional-private, or entity-at-controller-boundary pattern fails the ArchUnit gate"
    - "The module NAME SET is asserted dynamically — `bpm` is expected only when bpm.enabled=true"
    - "`verify --fast` includes backend-compile + archunit + modulith-verify and names the violated rule plus the fix on failure"
  artifacts:
    - path: "backend/pom.xml"
      provides: "Spring Boot 4.0.7 parent, Modulith 2.0.6 BOM, JDK 25, bpm.enabled property, bpm-off profile, ArchUnit 1.4.2 pin, Flyway Boot-4 starter pair, Surefire/Failsafe junit groups"
      contains: "spring-modulith-bom"
    - path: "backend/src/main/java/com/acme/app/shared/package-info.java"
      provides: "shared module boundary with allowedDependencies = {}"
      contains: "@ApplicationModule"
    - path: "backend/src/main/java/com/acme/app/shared/scheduling/ScheduledTask.java"
      provides: "sanctioned scheduling wrapper (replaces bare @Scheduled)"
    - path: "backend/src/main/java/com/acme/app/shared/query/NativeQuery.java"
      provides: "sanctioned native-query wrapper"
    - path: "backend/src/test/java/com/acme/app/ModulithVerifyTest.java"
      provides: "GATE-01 dynamic name-set assertion + modules.verify()"
      contains: "ApplicationModules.of"
    - path: "backend/src/test/java/com/acme/app/ArchitectureGatesTest.java"
      provides: "GATE-02 ArchUnit rule set with rule+fix messages"
      contains: "noFields()"
    - path: ".planning/spikes/q1-spring-cloud-consul-compat.md"
      provides: "Q-1 verdict: Spring Cloud release train compatible with Boot 4.0.x (or documented blocker)"
  key_links:
    - from: "scripts/checks/run-gate.mjs"
      to: "backend/pom.xml"
      via: "./mvnw subprocess gates (backend-compile, archunit, modulith-verify)"
      pattern: "mvnw"
    - from: "backend/src/test/java/com/acme/app/ModulithVerifyTest.java"
      to: "backend/src/main/java/com/acme/app/Application.java"
      via: "ApplicationModules.of(Application.class)"
      pattern: "ApplicationModules\\.of"
---

<objective>
Hand-build the Spring Boot 4 + Spring Modulith 2.0 Maven skeleton and the `shared` module as the REFERENCE SHAPE (D-01 manual-first), then wire the two architecture gates — Modulith verify (GATE-01) and ArchUnit (GATE-02) — into the verify spine so `./mvnw verify` fails the build on any boundary or banned-pattern violation. Also resolve the highest-risk open question first: verify which Spring Cloud release train is compatible with Boot 4.0.x (Q-1) BEFORE any Consul code is planned downstream.

Purpose: This is the foundation every other Phase 2 plan depends on. The `shared` reference shape is what the `new-module` skill is codified from (Plan 02), and the gates wired here are what every module-generating plan must keep green.
Output: Buildable `backend/` Maven project; `shared` module; live GATE-01 + GATE-02 in `run-gate.mjs`; Q-1 spike verdict.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-backend-foundation/02-CONTEXT.md
@.planning/phases/02-backend-foundation/02-RESEARCH.md
@.planning/phases/02-backend-foundation/02-PATTERNS.md
@CLAUDE.md
@backend/CLAUDE.md
@.cowork/tiers.json
@.planning/spikes/q004-verify-fast.md
@scripts/checks/run-gate.mjs
@Taskfile.yml
@.claude/skills/plan/SKILL.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Q-1 Spring Cloud Consul compatibility verification (Wave 0 risk)</name>
  <files>.planning/spikes/q1-spring-cloud-consul-compat.md</files>
  <read_first>
    - .planning/phases/02-backend-foundation/02-RESEARCH.md (Open Question Q-1; D-07; Consul section)
    - .planning/phases/02-backend-foundation/02-CONTEXT.md (D-07, D-08 — Consul dependency policy)
    - CLAUDE.md (Spring Boot 4.0.7 pin; "Not 4.1.0" rationale)
  </read_first>
  <action>
    Determine which Spring Cloud release train (and therefore which `spring-cloud-consul-config` / `spring-cloud-dependencies` BOM version) declares compatibility with Spring Boot 4.0.x. Use Context7 (resolve-library-id then query-docs for `spring-cloud-dependencies` Boot 4 compatibility) and, if Context7 lacks a definitive statement, fetch https://spring.io/projects/spring-cloud (release-train compatibility matrix) and the spring-cloud-consul project page. Record findings in a new spike report `.planning/spikes/q1-spring-cloud-consul-compat.md` with a single VERDICT line: either (a) COMPATIBLE — name the exact `spring-cloud-dependencies` BOM version to import in backend/pom.xml, or (b) BLOCKED — no Boot-4-compatible Consul train exists yet. Label every version claim with confidence ([VERIFIED]/[ASSUMED]) per the reality-filter rule; do not state a version as fact unless the source confirms it. If BLOCKED, the spike MUST state the fallback contract: FOUND-10's env+ConfigMap source path is the must-pass baseline, and the Consul KV source (D-07/D-08) becomes conditional — Plan 02 then ships typed-properties + env source only and Plan 05 omits the Consul Testcontainers test, tracking it as a deferred item.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const t=fs.readFileSync('.planning/spikes/q1-spring-cloud-consul-compat.md','utf8');if(!/VERDICT:\s*(COMPATIBLE|BLOCKED)/.test(t)){process.exit(1)}"</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/spikes/q1-spring-cloud-consul-compat.md` exists and contains a `VERDICT: COMPATIBLE` or `VERDICT: BLOCKED` line.
    - If COMPATIBLE: the exact `spring-cloud-dependencies` BOM version to use is named and confidence-labelled.
    - If BLOCKED: the env+ConfigMap fallback baseline and Consul-conditional contract are written for Plan 02 / Plan 05 to read.
  </acceptance_criteria>
  <done>Q-1 has a recorded verdict that downstream plans (02 Consul profile, 05 Consul test) can read; no Consul code lands before this verdict exists.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Maven skeleton + shared reference module + bound spec</name>
  <files>backend/pom.xml, backend/mvnw, backend/mvnw.cmd, backend/.mvn/wrapper/maven-wrapper.properties, backend/src/main/java/com/acme/app/Application.java, backend/src/main/java/com/acme/app/shared/package-info.java, backend/src/main/java/com/acme/app/shared/scheduling/ScheduledTask.java, backend/src/main/java/com/acme/app/shared/scheduling/ScheduledTaskRegistrar.java, backend/src/main/java/com/acme/app/shared/query/NativeQuery.java, backend/src/main/resources/application.yml, backend/src/test/resources/application-test.yml, specs/008-backend-skeleton-gates/spec.md, specs/008-backend-skeleton-gates/plan.md</files>
  <read_first>
    - .planning/phases/02-backend-foundation/02-PATTERNS.md (backend/pom.xml BOM/parent/Flyway/Modulith/ArchUnit/bpm-off/Surefire patterns; Application.java; shared package-info; ScheduledTask; NativeQuery)
    - .planning/phases/02-backend-foundation/02-RESEARCH.md (Standard Stack; Modulith Starter Artifacts; Flyway Boot 4 gotcha; Maven Layout single-module recommendation; Pattern 1)
    - CLAUDE.md (version pins: Boot 4.0.7, Modulith 2.0.6, Testcontainers 2.x, JUnit 6, ArchUnit ≥1.4.2, Maven 3.9.16; JDK 25 override)
    - backend/CLAUDE.md (module layout, communication rules, hallucination warnings)
    - .claude/skills/plan/SKILL.md (bound-spec format; Approved-by stays empty)
  </read_first>
  <behavior>
    - `./mvnw -q compile -f backend/pom.xml` succeeds on JDK 25.
    - `ApplicationModules.of(Application.class).verify()` passes with exactly the `shared` module discovered (no cycles, no undeclared deps).
    - The bpm-off Maven profile excludes `com/acme/app/bpm/**` from compile (no bpm package exists yet, so the profile is a no-op that still parses).
  </behavior>
  <action>
    Author the bound spec FIRST: create `specs/008-backend-skeleton-gates/spec.md` and `specs/008-backend-skeleton-gates/plan.md` via the `plan` skill format (tier T3 — pom.xml + package-info.java are T3 paths per .cowork/tiers.json; leave `Approved-by:` EMPTY for the H2 checkpoint). Bind the work branch to `feat/008-backend-skeleton-gates`. Then scaffold the Maven Wrapper (`mvnw`, `mvnw.cmd`, `.mvn/wrapper/maven-wrapper.properties` pinned to Maven 3.9.16) and `backend/pom.xml`: Boot parent 4.0.7, `<java.version>25</java.version>`, `<bpm.enabled>true</bpm.enabled>`, dependencyManagement importing `spring-modulith-bom` 2.0.6 and `testcontainers-bom` (Boot-managed), runtime deps `spring-boot-starter-web`, `spring-modulith-starter-jdbc`, `spring-modulith-starter-actuator`, `spring-boot-starter-flyway` + `org.flywaydb:flyway-database-postgresql` (both required — Boot 4 gotcha; do not pin versions), `org.postgresql:postgresql`; test deps `spring-modulith-starter-test`, `spring-boot-starter-test`, `com.tngtech.archunit:archunit-junit5:1.4.2` (pin explicitly — Boot does not manage it). Add the `bpm-off` profile excluding `com/acme/app/bpm/**` (D-05) and Surefire `systemPropertyVariables` passing `bpm.enabled=${bpm.enabled}` plus JUnit `groups`/`excludedGroups` config so `-Dgroups=unit` and `-Dgroups=integration` select test tags. Create `Application.java` (`@SpringBootApplication`). Create the `shared` module: `package-info.java` with `@ApplicationModule(allowedDependencies = {})`, `shared/scheduling/ScheduledTask.java` (interface: `name()`, `execute()`), `shared/scheduling/ScheduledTaskRegistrar.java` (the ONLY class allowed to carry `@Scheduled`, dispatching to registered `ScheduledTask` beans via constructor injection — satisfies the ArchUnit rule that confines `@Scheduled` to `com.acme.app.shared.scheduling..`), and `shared/query/NativeQuery.java` (sanctioned native-query wrapper interface). NO BaseEntity/tenant_id (Phase 3), NO ProblemDetail (Phase 4) per D-03. Use constructor injection everywhere; Jackson 3 (`tools.jackson.*`) if any JSON appears. Add minimal `application.yml` (Modulith EPR + actuator config stubbed; full EPR wiring lands in Plan 05) and `application-test.yml`.
  </action>
  <verify>
    <automated>./mvnw -q compile -f backend/pom.xml</automated>
  </verify>
  <acceptance_criteria>
    - `./mvnw -q compile -f backend/pom.xml` exits 0 on JDK 25.
    - `backend/pom.xml` imports `spring-modulith-bom` 2.0.6, pins `archunit-junit5` 1.4.2, includes BOTH `spring-boot-starter-flyway` and `flyway-database-postgresql`, sets `<java.version>25</java.version>`, declares the `bpm-off` profile and Surefire `bpm.enabled` system property.
    - `shared/package-info.java` declares `@ApplicationModule(allowedDependencies = {})`.
    - `@Scheduled` appears only inside `com.acme.app.shared.scheduling`.
    - `specs/008-backend-skeleton-gates/plan.md` exists with tier T3 and an EMPTY `Approved-by:` line; work branch is `feat/008-backend-skeleton-gates`.
  </acceptance_criteria>
  <done>The backend compiles; `shared` is the reference module shape; the bound T3 spec exists awaiting H2 approval.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: GATE-01 Modulith verify + GATE-02 ArchUnit tests, wired into run-gate.mjs</name>
  <files>backend/src/test/java/com/acme/app/ModulithVerifyTest.java, backend/src/test/java/com/acme/app/ArchitectureGatesTest.java, scripts/checks/run-gate.mjs, Taskfile.yml</files>
  <read_first>
    - .planning/phases/02-backend-foundation/02-PATTERNS.md (ModulithVerifyTest Pattern 2; ArchitectureGatesTest rules with rule+fix messages; run-gate.mjs extension pattern lines 642-699; Taskfile extension lines 702-716)
    - .planning/phases/02-backend-foundation/02-RESEARCH.md (Pattern 2 dynamic name-set; ArchUnit Gate Rules section; Q-3 JUnit-6 fallback; verify --fast gate-set table)
    - scripts/checks/run-gate.mjs (existing gates() function lines 34-73; mode handling lines 94-136)
    - Taskfile.yml (verify / verify:fast tasks lines 37-46)
    - .planning/spikes/q004-verify-fast.md (§7 growth rule — Phase 2 fast additions; §5 timing format; budget=gate)
  </read_first>
  <behavior>
    - GATE-01: with only `shared` present, the name-set assertion expects `{shared}` (+`bpm` when bpm.enabled=true) and `modules.verify()` passes; an injected fake cyclic/undeclared dependency makes it FAIL.
    - GATE-02: each ArchUnit rule FAILS when its banned pattern is introduced (field injection, bare @Scheduled outside the wrapper, native query outside the wrapper, @Transactional private, entity at controller boundary, @Value outside appconfig, Environment injection outside appconfig) and PASSES on the clean skeleton.
    - The gate failure message names the violated rule and the fix (AGENT-08 format).
  </behavior>
  <action>
    Write `ModulithVerifyTest.java` (RESEARCH Pattern 2): `BASE_MODULES = Set.of("shared")` for this plan's state; read `bpm.enabled` system property (default true) and add `"bpm"` to the expected set when true; call `ApplicationModules.of(Application.class).verify()` then assert the discovered module name set equals expected. Tag it as a unit/static test (no Spring context, no containers) so it joins `--fast`. Write `ArchitectureGatesTest.java` with the full GATE-02 rule set from PATTERNS (NO_FIELD_INJECTION, NO_BARE_SCHEDULED confined to `com.acme.app.shared.scheduling..`, NO_NATIVE_QUERY_OUTSIDE_WRAPPER confined to `com.acme.app.shared.query..`, NO_TRANSACTIONAL_PRIVATE, NO_ENTITY_IN_CONTROLLERS, plus the D-09 rules NO_VALUE_OUTSIDE_APPCONFIG and NO_ENVIRONMENT_INJECTION_OUTSIDE_APPCONFIG) — each `.because(...)` message MUST name the rule id and the concrete fix. Handle Q-3: if `@ArchTest`/`@AnalyzeClasses` do not run under JUnit 6, fall back to plain `@Test` methods calling `rule.check(importedClasses)` where `importedClasses` comes from a shared `ClassFileImporter().importPackages("com.acme.app")` — document which path was used in the SUMMARY. Then extend `scripts/checks/run-gate.mjs`: refactor `gates()` to accept a `mode` parameter and tag each entry `fast: true|false`; add the Phase 2 `--fast` gates `backend-compile`, `archunit`, `modulith-verify` (commands per RESEARCH verify --fast table and PATTERNS run-gate snippet, `-f backend/pom.xml`); leave `backend-unit`, `backend-integration`, `kill-listener-test` registration to Plan 05 (this plan only needs the two gate tests live). Update `main()` so `--fast` filters to fast gates and `--full` runs all; keep the existing TOTAL/budget contract. Update `Taskfile.yml` only if a `backend:build` helper is needed (gate-set itself stays in run-gate.mjs). pom.xml, run-gate.mjs, and Taskfile.yml are all T3 — these writes are covered by the bound spec from Task 2.
  </action>
  <verify>
    <automated>./mvnw -q test -Dtest=ModulithVerifyTest,ArchitectureGatesTest -f backend/pom.xml</automated>
  </verify>
  <acceptance_criteria>
    - `./mvnw -q test -Dtest=ModulithVerifyTest -f backend/pom.xml` passes; the asserted name set is `{shared}` (+`bpm` when bpm.enabled=true), driven by the `bpm.enabled` system property.
    - `./mvnw -q test -Dtest=ArchitectureGatesTest -f backend/pom.xml` passes on the clean skeleton; every rule carries a rule-id + fix in its `.because(...)`.
    - `node scripts/checks/run-gate.mjs --mode fast` lists `backend-compile`, `archunit`, `modulith-verify` and prints `GATE <name> <millis>ms <PASS|FAIL>` lines plus TOTAL (Q-004 §5 format); a failing ArchUnit rule produces the rule+fix block.
    - `--full` runs a strict superset of `--fast`.
  </acceptance_criteria>
  <done>GATE-01 and GATE-02 are live in the verify spine; the build fails on boundary or banned-pattern violations with rule-named, fix-bearing messages.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: H2 approval — skeleton, gates, Q-1 verdict</name>
  <files>specs/008-backend-skeleton-gates/plan.md, .planning/spikes/q1-spring-cloud-consul-compat.md</files>
  <action>Blocking human-verify checkpoint: present the built artifacts for review and pause for H2 approval; the executor performs no further writes until the resume signal. See <what-built>/<how-to-verify>/<resume-signal> below.</action>
  <what-built>
    The backend Maven skeleton, the `shared` reference module, GATE-01 (Modulith verify) and GATE-02 (ArchUnit) wired into `run-gate.mjs`, and the bound T3 spec `specs/008-backend-skeleton-gates/plan.md`. pom.xml, package-info.java, run-gate.mjs and Taskfile.yml are all T3 paths — this checkpoint is the H2 approval for those boundary changes.
  </what-built>
  <how-to-verify>
    1. Confirm JDK 25 is active: run `java -version` (must report 25).
    2. Run `./mvnw -q verify -f backend/pom.xml` — the skeleton builds and both gate tests pass.
    3. Run `node scripts/checks/run-gate.mjs --mode fast` — confirm `backend-compile`, `archunit`, `modulith-verify` appear with timing lines and TOTAL < 60000ms.
    4. Review `specs/008-backend-skeleton-gates/plan.md` (tier T3, files-to-touch, constitution rules). To APPROVE, fill the `Approved-by:` line yourself via the GitHub PR review (per AI-COWORK H2) — never let the agent fill it.
    5. Review `.planning/spikes/q1-spring-cloud-consul-compat.md` VERDICT and confirm whether downstream Consul work (Plan 02 profile, Plan 05 test) proceeds or is deferred.
  </how-to-verify>
  <resume-signal>Type "approved" (and confirm the Q-1 verdict path) or describe issues to fix.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build → main branch | T3 boundary writes (pom.xml, package-info.java, gate config) must carry an approved bound spec; the hook + plan-compliance CI enforce it |
| npm/Maven registry → build | dependency resolution pulls Maven artifacts and (Task 1) consults Spring Cloud release-train metadata |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | Architecture gates (ArchUnit/Modulith) bypassed or weakened to get green | mitigate | Gate scripts + pom are T3; the verify skill's "fix the code never the gate" rule + plan-compliance CI block weakening; gate tests assert rules FIRE on violations (Task 3 behavior) |
| T-02-02 | Tampering | Unverified Spring Cloud / Consul dependency pinned on guesswork (supply-chain + breakage) | mitigate | Task 1 records a confidence-labelled verdict before any Consul artifact is added; BLOCKED path defers Consul rather than pinning an unverified version |
| T-02-03 | Repudiation | T3 boundary change merged without traceable approver | mitigate | Bound spec `specs/008-*/plan.md` with `Approved-by:` filled only via PR review (H2); hook + L2 plan-compliance CI verify approver via hosting API |
| T-02-SC | Tampering | Maven/npm installs (pom dependency additions, maven-wrapper download) | mitigate | All version pins traced to CLAUDE.md (authoritative research) or Boot-managed BOM; ArchUnit pinned explicitly to 1.4.2; human checkpoint reviews the dependency set; no `[ASSUMED]`/`[SUS]` package added without the Task 1 verdict |
</threat_model>

<verification>
- `./mvnw -q verify -f backend/pom.xml` builds the skeleton and passes ModulithVerifyTest + ArchitectureGatesTest.
- `node scripts/checks/run-gate.mjs --mode fast` shows the three new backend fast gates with the Q-004 timing format and TOTAL < 60000ms.
- `.planning/spikes/q1-spring-cloud-consul-compat.md` has a VERDICT line.
- The bound T3 spec exists with an empty `Approved-by:` (filled by human at the checkpoint).
</verification>

<success_criteria>
- Backend compiles on JDK 25 with the locked stack (Boot 4.0.7 / Modulith 2.0.6 / ArchUnit 1.4.2 / Flyway Boot-4 pair).
- GATE-01 fails on undeclared deps / DAG cycles; GATE-02 fails on each banned pattern, with rule+fix messages (AGENT-08).
- Module name-set assertion is dynamic by `bpm.enabled` (D-06).
- `verify --fast` includes the three compile/static backend gates and stays under the 60s budget.
- Q-1 verdict recorded; Consul path is either greenlit (COMPATIBLE) or deferred with a documented baseline (BLOCKED).
</success_criteria>

<artifacts_this_phase_produces>
- Modules: `com.acme.app.shared` (`@ApplicationModule(allowedDependencies = {})`).
- Entry point: `com.acme.app.Application` (`@SpringBootApplication`).
- Shared types: `shared.scheduling.ScheduledTask`, `shared.scheduling.ScheduledTaskRegistrar`, `shared.query.NativeQuery`.
- Tests: `ModulithVerifyTest` (GATE-01), `ArchitectureGatesTest` (GATE-02 rules: NO_FIELD_INJECTION, NO_BARE_SCHEDULED, NO_NATIVE_QUERY_OUTSIDE_WRAPPER, NO_TRANSACTIONAL_PRIVATE, NO_ENTITY_IN_CONTROLLERS, NO_VALUE_OUTSIDE_APPCONFIG, NO_ENVIRONMENT_INJECTION_OUTSIDE_APPCONFIG).
- Build: `backend/pom.xml` (bpm.enabled property, bpm-off profile, Surefire bpm.enabled passthrough), Maven Wrapper files.
- Gate runner additions: `run-gate.mjs` gates `backend-compile`, `archunit`, `modulith-verify` (fast).
- Config: `application.yml`, `application-test.yml` (EPR/actuator stubs; full wiring in later plans).
- Spec: `specs/008-backend-skeleton-gates/{spec,plan}.md`.
- Spike: `.planning/spikes/q1-spring-cloud-consul-compat.md`.
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/02-backend-foundation/02-01-SUMMARY.md` when done.
</output>
